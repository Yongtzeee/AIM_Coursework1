# customized code
# -*- coding: utf-8 -*-
"""gbndm_aim_0_3_9.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1Ny3YOI8G7xTqCFgCdqpdkZvI6yrfwdIe
"""

#version = '_v0p3p9_'

from __future__ import print_function
import argparse
import torch.nn as nn
import torch.nn.functional as F
import torch.optim as optim

import time
import warnings

import numpy as np
import matplotlib.pyplot as plt
import matplotlib.mlab as mlab

from sklearn import cluster, datasets, mixture
from sklearn.neighbors import kneighbors_graph
from sklearn.preprocessing import StandardScaler
from itertools import cycle, islice

import torch
import numpy as np
from torch.utils import data
import copy

from torchvision import datasets, transforms
from torch.autograd import Variable

from IPython.core.debugger import set_trace

# ===============
# Initializations
# ===============

#!pip install torch torchvision

# -----------------------------------------------------------------------------------------------------------

# ===============
# Parameters
# ===============

# --- General framework arguments

args = {}
kwargs = {}
args['num_train_batch'] = 1  # number of MNIST training batches
args['num_valid_batch'] = 10  # number of MNIST validation batches
args['train_batch_size'] = 100   # training batch size
args['valid_batch_size'] = 100   # validation batch size
args['test_batch_size'] = 10
# args['epochs'] = 100  # number of training epochs 
args['lr'] = 1 # 0.1  # 0.01 # learning rate # this is over-written by the solution
args['momentum'] = 0.5 # SGD momentum (default: 0.5); momentum is a moving average of our gradients (helps keep a useful direction)
args['clip_level'] = 0.5  # gradient clip threshold
args['seed']= 1 #random seed
args['log_interval_epoch'] = 1 # display training loss every log_int... epochs
args['cuda'] = True 
args['patience'] = 1000  # stop train. if the valid. err. hasn't improved by this num. of epochs
args['noise_in'] = -1   # 0.5  # 0.15  # amount of noise to add to the training data
args['noise_out'] = -1   #  probability of changing an output label to some random label
args['verbose_train'] = False   # print status of model training?
args['verbose_meta'] = True #  print status of architecture optimization?
args['min_inst_class'] = 5 # minimum number of instances per class in the training set
args['batch_max_tries'] = 10 # max. num. of attempts in extracting data batches
args['save_best_chrom'] = False # False  # save the best chromose in Google Drive?
args['dc_prob_drop'] = 0.5 # 0.5 # probability of dropping a circuit (DropCircuit)
args['prob_sel_branch'] = 0.5 # probability of architectural search selecting/using a branch (this is not DropCircuit) 

# --- Neural architectural limits

args['num_epochs_search'] = 10 # 100  # number of epochs for training during architecture search 
args['num_epochs_test'] = 1000  # num. epochs for training during the final test
limits = {}
limits['min_layer_nodes'] =  10 # 5 # 50
limits['max_layer_nodes'] = 100
limits['max_pre_branch_layers'] = 5 # 3
limits['max_branches'] = 10
limits['max_branch_layers'] = 5  # 3
limits['max_post_branch_layers'] = 5 # 3
limits['lr1_min'] = 0.001
limits['lr1_,max'] = 2
limits['lr2_min'] = 0.001
limits['lr2_,max'] = 2
limits['moment_min'] = 0.001
limits['moment_max'] = 1

# np.random.seed(0)
# torch.manual_seed(0)

num_train_instances = args['num_train_batch'] * args['train_batch_size']
num_valid_instances = args['num_valid_batch'] * args['valid_batch_size']

data_rand_seed = 1 # (other pre-tested seeds: 2, 3)
do_eval_iter = 0

# -----------------------------------------------------------------------------------------------------------

# ============
# Load dataset
# ============

# Seed creation
torch.manual_seed(data_rand_seed)
np.random.seed(data_rand_seed)

a_data_transform = transforms.Compose([transforms.ToTensor(), transforms.Normalize((0.1307,), (0.3081,))])
  
train_loader = torch.utils.data.DataLoader(
    datasets.MNIST('../data', train=True, download=True,
                   transform=a_data_transform),
    batch_size=args['train_batch_size'], shuffle=True, **kwargs)

test_loader = torch.utils.data.DataLoader(
    datasets.MNIST('../data', train=False, transform=a_data_transform),
    batch_size=args['test_batch_size'], shuffle=True, **kwargs)

# Check whether we have enough instances per class
# We want an imbalanced dataset but we don't want any specific label having
# an "insufficient" number of instances.
def check_enough_inst(batches, min_inst_per_class):
  
  # Concatenate training batch output labels
  labels = batches[0][1].numpy()
  for batch_i in range(1,args['num_train_batch']):
    new_labels = batches[batch_i][1].numpy()
    labels = np.concatenate((labels, new_labels))

  labels = labels.tolist()

  # Scan though labels
  for a_label in range(10):  # assuming MNIST, of course
    a_count = labels.count(a_label)
    # If label count is < min_inst_per_class return False
    if a_count < min_inst_per_class:
      return False

  # Return True
  return True


# Prepare data. Extract training and validation batches
# This is where we make the problem "small and imbalanced"
def extract_batches(a_loader, min_inst_per_class, max_tries):

  # Keep trying until you have a required minimum number of instances 
  # per class in the training set (not elegant but ok for the range of 
  # "min_inst_per_class" we need)

  for ti in range(max_tries):

    print('Data extraction trial {}.'.format(ti))

    # Initializations
    train_batches = []
    valid_batches = []
    tot_batch_extract = args['num_train_batch'] + args['num_valid_batch']

    # Extract
    for batch_idx, (data, target) in enumerate(a_loader):

      if batch_idx < args['num_train_batch']:
        train_batches.append((data,target))
      else:
        valid_batches.append((data,target))
        if batch_idx == tot_batch_extract - 1:
          break

    # Check minimum number of instances
    enough_instaces = check_enough_inst(train_batches, min_inst_per_class)
    if enough_instaces:
      return train_batches, valid_batches

  print('It was not possible to create a dataset.')
  print('Consider increasing the overall number of instances, or')
  print('decreasing the minimum instances per class allowed.')
  return [], []


train_batches, valid_batches = extract_batches(train_loader, args['min_inst_class'], args['batch_max_tries'])
if args['verbose_train']:
  print('Extracted {} train_batches, and {} valid_batches.'.format(len(train_batches), len(valid_batches)))

# -----------------------------------------------------------------------------------------------------------

# ===========================
# Display histogram of labels
# ===========================

def disp_hist_labaels(batches):
  # Concatenate training batch output labels
  labels = batches[0][1].numpy()		# batches[0][0] are the pictures, batches[0][1] are labels for each picture
  for batch_i in range(1,args['num_train_batch']):
    new_labels = batches[batch_i][1].numpy()
    labels = np.concatenate((labels, new_labels))

  num_bins = 10
  n, bins, patches = plt.hist(labels, num_bins, facecolor='blue', alpha=0.5)
  plt.show()

disp_hist_labaels(train_batches)

# ==========================
# Display dataset
# ==========================

# This is not currently used

# Display MNIST instances
# Adapted from # https://github.com/CSCfi/machine-learning-scripts/blob/master/notebooks/pytorch-mnist-mlp.ipynb
def disp_MNIST_inst(num_disp, X_train, y_train):
  pltsize=1
  plt.figure(figsize=(num_disp*pltsize, pltsize))
  for i in range(num_disp):
    plt.subplot(1,num_disp,i+1)
    plt.axis('off')
    plt.imshow(X_train[i,:,:].numpy().reshape(28,28), cmap="gray_r")
    plt.title('Class: '+str(y_train[i].item()))

if args['verbose_train']:
  X_train = train_batches[0][0]
  y_train = train_batches[0][1]

  disp_MNIST_inst(10, X_train, y_train)

  sum_train_0 = X_train[0,:,:].sum()
  min_train_0 = X_train[0,:,:].min()
  max_train_0 = X_train[0,:,:].max()

  print('X_train[0,:,:] --> sum ({}); min ({}); max ({}).'.format(sum_train_0, min_train_0, max_train_0))

# -----------------------------------------------------------------------------------------------------------

# ==========================
# Design model
# ==========================

# "Unseed" the rest
np.random.seed()
a_rand_seed = np.random.randint(0,999999)
torch.manual_seed(a_rand_seed)

# Simple function to scale parameters
# num is assume to be \in [0,1]
def scale_to_range(num,min_val,max_val):
  range = max_val - min_val
  return (num*range)+min_val

# Simple layer for doing elementwise scaling
# Adapated from https://stackoverflow.com/questions/51980654/pytorch-element-wise-filter-layer
class Elem_Scaling_1D(nn.Module):
  def __init__(self, num_nodes, bogus):  # clean-up "bogus"
    super(Elem_Scaling_1D, self).__init__()
    # Initialize
    init_weights = np.random.normal(loc=0, scale=0.5, size=np.shape(num_nodes))
    # Assign
    self.weights = torch.from_numpy(init_weights)
    #self.weights = nn.Parameter(torch.Tensor(1, num_nodes))  # trainable parameter

  def forward(self, x):
    # assuming x is of size num_inst-1-num_nodes
    return x * self.weights  # element-wise multiplication

# Class gradient-based neural diversity machine
class GBNDM(nn.Module):
    def __init__(self, a_chromosome):   # assuming MNIST
        super(GBNDM, self).__init__()

        # --------- Pre-branch layers 
        self.chromosome = a_chromosome
        self.pre_branch_layers = nn.ModuleList()
        prev_num_out = 28*28
        chrom_ind = 5 # skip over 3 learning rate param., 1 moment. p. (interp./used in training)
        # and 1 exist-or-not (may use in future versions).
        # Each layer is [exist-or-not, id-or-linear, activation function (AF), 2 AF parameters, number of nodes → total: 6 parameters]
        for i in range(limits['max_pre_branch_layers']):
          
          # Extract and interpret parameters
          layer_params_raw = a_chromosome[chrom_ind:chrom_ind+6]
          layer_params_real = self.interp_layer_param(layer_params_raw)
        
          # Decide on whether to create a layer or not
          if i==0:  # the first layer of each segment is done by default
            do_layer = True
          else:
            do_layer = layer_params_real[0]

          if do_layer:
            # Create layer    
            a_layer, prev_num_out = self.create_layer(prev_num_out, layer_params_real)
            # Append layer and update chromosome index
            self.pre_branch_layers.append(a_layer)
            
          chrom_ind += 6

        # --------- Branches
        num_out_pre_branch = prev_num_out

        self.branches = nn.ModuleList()

        # Scan over branches
        final_num_nodes = []  # keep track of the size of the final layer of each branch
        count_branches = 0
        for bi in range(limits['max_branches']):

          # Initialize branch
          branch_layers = nn.ModuleList()

          # Do branch? Always do the first one by default
          if (a_chromosome[chrom_ind] < args['prob_sel_branch']) or (bi == 0):
            do_branch = True
          else: 
            do_branch = False
            
          chrom_ind += 1

          # Scan over branch layers

          if do_branch:  # if doing branch

            this_fin_num_nodes = 0

            count_branches += 1
            
            for li in range(limits['max_branch_layers']):

              # Extract and interpret parameters
              layer_params_raw = a_chromosome[chrom_ind:chrom_ind+6]
              layer_params_real = self.interp_layer_param(layer_params_raw)

              # Decide on whether to create a layer or not
              if li==0:  # the first layer of each branch is done by default
                do_layer = True
                prev_num_out = num_out_pre_branch
              else:
                do_layer = layer_params_real[0]

              if do_layer:
                # print('... temp ... this_fin_num_nodes: {}.'.format(this_fin_num_nodes))
                # Create layer    
                a_layer, prev_num_out = self.create_layer(prev_num_out, layer_params_real)
                # Num_nodes - keep storing the latest one; the last latest is the final layer num_nodes
                this_fin_num_nodes = prev_num_out
                # Append layer and update chromosome index
                branch_layers.append(a_layer)

              chrom_ind += 6

            # Append branch
            final_num_nodes.append(this_fin_num_nodes)
            #print('final_num_nodes: {}.'.format(final_num_nodes))
            self.branches.append(branch_layers)

          else: # if not doing branch you still need to increment chromosome index

            chrom_ind += 6*limits['max_branch_layers']

        self.num_branches = count_branches
        self.dc_prob_activ = 1 - args['dc_prob_drop'] # probability of using a circuit
        self.tot_nodes_branch_final = sum(final_num_nodes)
        
        # --------- Post-branch layers

        prev_num_out = self.tot_nodes_branch_final
        
        # --- Create post-branch layers

        chrom_ind += 1  # skip over the parameter pertaining to the existence or not of the post-branch segment

        self.post_branch_layers = nn.ModuleList()
        
        # Each layer is [exist-or-not, id-or-linear, activation function (AF), 2 AF parameters, number of nodes → total: 6 parameters]
        for li in range(limits['max_post_branch_layers']):
          
          # Extract and interpret parameters
          layer_params_raw = a_chromosome[chrom_ind:chrom_ind+6]
          layer_params_real = self.interp_layer_param(layer_params_raw)
        
          # Decide on whether to create a layer or not
          if li==0:  # the first layer of each segment is done by default
            do_layer = True
          else:
            do_layer = layer_params_real[0]

          if do_layer:
            # Create layer    
            a_layer, prev_num_out = self.create_layer(prev_num_out, layer_params_real)
            # Append layer and update chromosome index
            self.post_branch_layers.append(a_layer)
            
          chrom_ind += 6

        # Create a final layer
        self.final_layer = nn.Linear(prev_num_out, 10)

    def forward(self, x, dc_mask = None):
        
        x = x.view(-1, 28*28)

        # Apply pre-branch layers
        for pi, a_layer in enumerate(self.pre_branch_layers):
          #print('Pre-layer {}'.format(pi))
          # if isinstance(a_layer, Elem_Scaling_1D):
          #   set_trace()
          x = a_layer(x)

        # Apply branches
        branch_finals = []
        for bi, a_branch in enumerate(self.branches):
          #print('Branch {}'.format(bi))
          
          z = x

          for a_layer in a_branch:
            # if isinstance(a_layer, Elem_Scaling_1D):
            #   set_trace()
            z = a_layer(z)
          
          if self.training:
            z = (dc_mask[bi] * z) / self.dc_prob_activ  # if training apply DropCircuit
          
          branch_finals.append(z)

        # Concatenate multi-branch final layers
        x = torch.cat(branch_finals,dim=1)

        # if not(self.training): # if not training then testing/evaluating
        #   x = self.dc_prob_activ * x  # scaling due to DropCircuit
      

        # Apply post-branch layers
        for pi, a_layer in enumerate(self.post_branch_layers):
          #print('Post-layer: {}'.format(pi))
          # if isinstance(a_layer, Elem_Scaling_1D):
          #   set_trace()
          x = a_layer(x)

        # Apply final layer
        x = self.final_layer(x)
               
        return F.log_softmax(x, dim=1)


    # Method for interpreting layer parameters
    # Each layer is [exist-or-not, id-or-linear, activation function (AF), 2 AF parameters, number of nodes → total: 6 parameters]
    def interp_layer_param(self, layer_params_raw):
      # Exist or not
      if layer_params_raw[0] < 0.5:
        exist = False
      else:
        exist = True
      # Weight function
      tot_weight_func = 3
      if layer_params_raw[1] < 0.6: # or ... (1/tot_weight_func):  
        weight_func_sel = 'linear'
      elif layer_params_raw[1] < 0.8: # or ... (2/tot_weight_func):
        weight_func_sel = 'elem_scale'
      else:
        weight_func_sel = 'id'
      # activation function
      tot_node_func = 22
      if layer_params_raw[2] < (1/tot_node_func):
        activ_func = 'ReLU'
        param_1 = -1
        param_2 = -1
      elif layer_params_raw[2] < (2/tot_node_func):
        activ_func = 'Hardshrink'
        param_1 = scale_to_range(layer_params_raw[3],0,2)
        param_2 = -1
      elif layer_params_raw[2] < (3/tot_node_func):
        activ_func = 'Hardtanh'
        param_1 = scale_to_range(layer_params_raw[3],0,2)
        param_2 = scale_to_range(layer_params_raw[4],0,2)
        if param_1 > param_2:  # param_1 is min_val; param_2 is max_val
          tmp_val = param_1
          param_1 = param_2
          param_2 = tmp_val
        elif param_1 == param_2:
          param_2 += 0.1
      elif layer_params_raw[2] < (4/tot_node_func):
        activ_func = 'LeakyReLU'
        param_1 = scale_to_range(layer_params_raw[3],0,1)
        param_2 = -1
      elif layer_params_raw[2] < (5/tot_node_func):
        activ_func = 'LogSigmoid'
        param_1 = -1
        param_2 = -1
      elif layer_params_raw[2] < (6/tot_node_func):
        activ_func = 'PReLU'
        param_1 = -1
        param_2 = -1
      elif layer_params_raw[2] < (7/tot_node_func):
        activ_func = 'ELU'
        param_1 = scale_to_range(layer_params_raw[3],0,2)
        param_2 = -1
      elif layer_params_raw[2] < (8/tot_node_func):
        activ_func = 'ReLU6'
        param_1 = -1
        param_2 = -1
      elif layer_params_raw[2] < (9/tot_node_func):
        activ_func = 'RReLU'
        param_1 = -1
        param_2 = -1
      elif layer_params_raw[2] < (10/tot_node_func):
        activ_func = 'SELU'
        param_1 = -1
        param_2 = -1
      elif layer_params_raw[2] < (11/tot_node_func):
        activ_func = 'CELU'
        param_1 = scale_to_range(layer_params_raw[3],0,2)
        param_2 = -1
      elif layer_params_raw[2] < (12/tot_node_func):
        activ_func = 'GELU'
        param_1 = -1
        param_2 = -1
      elif layer_params_raw[2] < (13/tot_node_func):
        activ_func = 'Sigmoid'
        param_1 = -1
        param_2 = -1
      elif layer_params_raw[2] < (14/tot_node_func):
        activ_func = 'Softplus'
        param_1 = scale_to_range(layer_params_raw[3],0,2)
        param_2 = scale_to_range(layer_params_raw[4],0,40)
      elif layer_params_raw[2] < (15/tot_node_func):
        activ_func = 'Softshrink'
        param_1 = scale_to_range(layer_params_raw[3],0,2)
        param_2 = -1
      elif layer_params_raw[2] < (16/tot_node_func):
        activ_func = 'Softsign'
        param_1 = -1
        param_2 = -1
      elif layer_params_raw[2] < (17/tot_node_func):
        activ_func = 'Tanh'
        param_1 = -1
        param_2 = -1
      elif layer_params_raw[2] < (18/tot_node_func):
        activ_func = 'Tanhshrink'
        param_1 = -1
        param_2 = -1
      elif layer_params_raw[2] < (19/tot_node_func):
        activ_func = 'Threshold'
        param_1 = scale_to_range(layer_params_raw[3],0,2)
        param_2 = scale_to_range(layer_params_raw[4],0,2)
      elif layer_params_raw[2] < (20/tot_node_func):
        activ_func = 'Softmin'
        param_1 = -1
        param_2 = -1
      elif layer_params_raw[2] < (21/tot_node_func):
        activ_func = 'Softmax'
        param_1 = -1
        param_2 = -1
      else: 
        activ_func = 'None'
        param_1 = -1
        param_2 = -1

      # number of nodes
      num_nodes = scale_to_range(layer_params_raw[5], limits['min_layer_nodes'], limits['max_layer_nodes'])
      num_nodes = num_nodes.astype(int)

      return (exist, weight_func_sel, activ_func, param_1, param_2, num_nodes)

    # Method for creating a layer
    # layer_params_real format: (exist, weight_func_sel, activ_func, param_1, param_2, num_nodes)
    def create_layer(self, prev_num_out, layer_params_real):
      
      exist, weight_func_sel, activ_func, param_1, param_2, num_nodes = layer_params_real
      
      # wf_param1/wf_param2 --> not elegant 
      if weight_func_sel == 'linear':
        weight_func = nn.Linear
        num_nodes_in = prev_num_out
        num_nodes_out = num_nodes
        wf_param1 = num_nodes_in
        wf_param2 = num_nodes_out
      elif weight_func_sel == 'id':
        weight_func = nn.Identity
        num_nodes_in = prev_num_out
        num_nodes_out = prev_num_out
        wf_param1 = num_nodes_in
        wf_param2 = num_nodes_out
      else:  # 'elem_scale'
        weight_func = Elem_Scaling_1D
        num_nodes_in = prev_num_out
        num_nodes_out = prev_num_out
        wf_param1 = num_nodes_in
        wf_param2 = num_nodes_out
        
      if activ_func == 'ReLU':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.ReLU())
      elif activ_func == 'Hardshrink':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.Hardshrink(param_1))
      elif activ_func == 'Hardtanh':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.Hardtanh(param_1, param_2))
      elif activ_func == 'LeakyReLU':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.LeakyReLU(param_1))
      elif activ_func == 'LogSigmoid':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.LogSigmoid())
      elif activ_func == 'PReLU':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.PReLU())
      elif activ_func == 'ELU':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.ELU(param_1))
      elif activ_func == 'ReLU6':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.ReLU6())
      elif activ_func == 'RReLU':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.RReLU())
      elif activ_func == 'SELU':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.SELU())
      elif activ_func == 'CELU':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.CELU(param_1))
      elif activ_func == 'GELU':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.ReLU())  # for some reason GELU is not present; revert later if relevant
      elif activ_func == 'Sigmoid':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.Sigmoid())
      elif activ_func == 'Softplus':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.Softplus(param_1, param_2))
      elif activ_func == 'Softshrink':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.Softplus(param_1))
      elif activ_func == 'Softsign':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.Softsign())
      elif activ_func == 'Tanh':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.Tanh())
      elif activ_func == 'Tanhshrink':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.Tanhshrink())
      elif activ_func == 'Threshold':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.Threshold(param_1, param_2))
      elif activ_func == 'Softmin':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.Softmin())
      elif activ_func == 'Softmax':
        a_layer = nn.Sequential(
            weight_func(wf_param1, wf_param2),
            nn.Softmax())
      else:
        a_layer = weight_func(wf_param1, wf_param2)

      return a_layer, num_nodes_out

# -----------------------------------------------------------------------------------------------------------
# Generating chromosomes

# Generate a random chromosome where each param. is \in [0,1)
def gen_rand_chromosome(num_param):
  chrom = np.random.rand(num_param)
  return chrom

# -----------------------------------------------------------------------------------------------------------

# ==========================
# Train and test functions
# ==========================

# Create a random mask for DropCircuit
def make_mask(num_circuits, prob_drop):
  rand_vals = np.random.rand(num_circuits)
  decisions = rand_vals >= prob_drop
  a_mask = np.ones(num_circuits)*decisions
  # Must have at least one circuit active
  if np.sum(a_mask) == 0:
    rand_index = np.random.randint(num_circuits)
    a_mask[rand_index] = 1.0

  return torch.from_numpy(a_mask)

def train_one_epoch(a_model, optimizer, epoch, batches):

  a_model.train()
  for batch_idx, (data, target) in enumerate(batches):
      if args['cuda']:
          data, target = data.cuda(), target.cuda()
      # Variables in Pytorch are differentiable. 
      
      data, target = Variable(data), Variable(target)
      #This will zero out the gradients for this batch. 
      optimizer.zero_grad()
      
      # Create DropCircuit mask
      dc_mask = make_mask(a_model.num_branches, args['dc_prob_drop'])
      
      output = a_model(data, dc_mask)

      # Calculate the negative log likelihood loss.
      loss = F.nll_loss(output, target)
      #dloss/dx for every Variable 
      loss.backward()
      # Gradient clipping
      torch.nn.utils.clip_grad_norm_(a_model.parameters(), args['clip_level'])
      #to do a one-step update on our parameter.
      optimizer.step()
      #Print out the loss periodically. 

  if args['verbose_train']:
    if epoch % args['log_interval_epoch'] == 0:
      print('Epoch: {}. Latest loss: {}.'.format(epoch, loss.data))

# Compute accuracy
# The argument (data_source) of this function can be a data_loader or a list of batches (previously extracted)
def comp_accuracy(a_model, data_source, src_num_instances):
    a_model.eval()
    a_loss = 0
    correct = 0
    preds = torch.zeros(0)
    first = True
    for a_data_in, a_data_out in data_source:
        if args['cuda']:
            a_data_in, a_data_out = a_data_in.cuda(), a_data_out.cuda()
        a_data_in, a_data_out = Variable(a_data_in), Variable(a_data_out)
        output = a_model(a_data_in)

        a_loss += F.nll_loss(output, a_data_out, reduction='sum').data # sum up batch loss
        pred = output.data.max(1, keepdim=True)[1] # get the index of the max log-probability
        if first:
          preds = pred
          first = False
        else:
          preds = torch.cat((preds, pred),0)

        correct += pred.eq(a_data_out.data.view_as(pred)).long().cpu().sum()

    # Compute and return accuracy
    if type(data_source) == list:  # case: list of batches
      result = 100. * (correct.numpy()/ src_num_instances)
    else: # case: data_loader
      result = 100. * (correct.numpy()/ len(data_source.dataset))
    return result

# -----------------------------------------------------------------------------------------------------------

# ==========================
# Evaluate and Train functions
# ==========================

# Compute the number of parameters in a chromose (depends on limits)
def comp_num_chrom_param(limits):
  # Num. of training parameters
  tot_train_param = 4 # lr1, lr2, lr2 prop, momentum
  # Num. of pre-branch parameters
  tot_pre_branch = 1+(6*limits['max_pre_branch_layers']) # exist-or-not, layer params.
  # Num. of branch parameters
  tot_branch = (1+(6*limits['max_branch_layers']))*limits['max_branches']
  # Num. of post-branch parameters
  tot_post_branch = 1+(6*limits['max_post_branch_layers'])

  # 4 + 31 + 310 + 31 = 376
  return tot_train_param+tot_pre_branch+tot_branch+tot_post_branch

# Interpret learning rate and momentum parameters
def interp_lrm(params):
  lr1 = scale_to_range(params[0], limits['lr1_min'], limits['lr1_,max'])
  lr2 = scale_to_range(params[1], limits['lr2_min'], limits['lr2_,max'])
  if lr1 < lr2:
    temp = lr1
    lr1 = lr2
    lr2 = temp
  lr2_epoch = (np.round(params[2]*args['num_epochs_search'])).astype(int)
  a_decr = (lr1-lr2)/lr2_epoch
  a_momentum = scale_to_range(params[3], limits['moment_min'], limits['moment_max'])
  return lr1, lr2, lr2_epoch, a_momentum, a_decr

# Chromosome evaluation (more efficient than do_training)
# Function to train a specific model
# Early stopping, or returning best validation model, is not implemented 
def do_eval_chrom(a_model, train_params, num_epochs):
  global do_eval_iter
  do_eval_iter += 1

  # Extract basic information
  lr1, lr2, lr2_epoch, a_momentum, lr_decr = train_params  
  args['lr'] = lr1
  args['momentum'] = a_momentum
  
  optimizer = optim.SGD(a_model.parameters(), lr=args['lr'], momentum=args['momentum'])
  valid_accurs = []
  train_accurs = []
  train_start_time = time.time()
  for epoch in range(1, num_epochs + 1):

    if args['verbose_train']:
      print('Epoch {} learning rate: {}.'.format(epoch, args['lr']))

    train_one_epoch(a_model, optimizer, epoch, train_batches)
    
    a_train_accur = comp_accuracy(a_model, train_batches, num_train_instances)
    if args['verbose_train']:
      print('Training accuracy: {}%.'.format(a_train_accur))
    train_accurs.append(a_train_accur)
    
    a_valid_accur = comp_accuracy(a_model, valid_batches, num_valid_instances)
    if args['verbose_train']:
      print('Validation accuracy: {}%.'.format(a_valid_accur))
    valid_accurs.append(a_valid_accur)

    # Decrement learning rate
    if epoch < lr2_epoch:
        args['lr'] -= lr_decr
        for param_group in optimizer.param_groups:
          param_group['lr'] = args['lr']

  train_elapsed_time = time.time() - train_start_time
  print('The training process took {} seconds.'.format(train_elapsed_time))

  return a_model, valid_accurs, train_accurs

# Function to train a specific model
# Early stopping, or returning best validation model, is not implemented 
def do_training(a_model, train_params, num_epochs):

  lr1, lr2, lr2_epoch, a_momentum, lr_decr = train_params  
  args['lr'] = lr1
  args['momentum'] = a_momentum

  optimizer = optim.SGD(a_model.parameters(), lr=args['lr'], momentum=args['momentum'])
  best_valid_accur = 0
  best_model = type(a_model)(a_model.chromosome) # get a new instance
  valid_accurs = []
  train_accurs = []
  train_start_time = time.time()
  for epoch in range(1, num_epochs + 1):

    if args['verbose_train']:
      print('Epoch {} learning rate: {}.'.format(epoch, args['lr']))

    train_one_epoch(a_model, optimizer, epoch, train_batches)
    
    a_train_accur = comp_accuracy(a_model, train_batches, num_train_instances)
    if args['verbose_train']:
      print('Training accuracy: {}%.'.format(a_train_accur))
    train_accurs.append(a_train_accur)
    
    a_valid_accur = comp_accuracy(a_model, valid_batches, num_valid_instances)
    if args['verbose_train']:
      print('Validation accuracy: {}%.'.format(a_valid_accur))
    valid_accurs.append(a_valid_accur)
    if a_valid_accur > best_valid_accur:
      best_valid_accur = a_valid_accur
      # best_model.load_state_dict(a_model.state_dict()) # copy weights and stuff
      best_model = copy.deepcopy(a_model)

    # Decrement learning rate
    if epoch < lr2_epoch:
        args['lr'] -= lr_decr
        for param_group in optimizer.param_groups:
          param_group['lr'] = args['lr']

  train_elapsed_time = time.time() - train_start_time
  print('The training process took {} seconds.'.format(train_elapsed_time))

  if args['cuda']:
      best_model.cuda()

  return a_model, best_model, valid_accurs, train_accurs

# -----------------------------------------------------------------------------------------------------------

# ================================
# Architectural search
# ================================

# Artificial Intelligence Methods students should focus on the code below.
# You should keep the neural network code unchanged. This is crucial for 
# comparison purposes. In other words, focus only on modifying the architecture
# search code below.

import random
import math

# --- Architectural search parameters
meta = {}
meta['max_rs_iter'] = 10 # 6 # 10  # initial random search
meta['max_shc_iter'] = 20 # 40 # 20 # 40  # stochastic hill climbing iterations
meta['num_differential_sol'] = 4 # 8 # number of differential evolution solutions
meta['diff_lr'] = 0.4 # 0.5 # 0.1 # learning rate for differential search
meta['num_neighbours'] = 8 # 16 
meta['neighbour_range'] = 0.2  # 0.4  # mutation rate for stochastic hill-climbing

# --- Population based search parameters (genetic algorithm)
# meta['population_search_iter'] = 6  # population based genetic algorithm search iteration
meta['mutation_rate'] = 0.2 # mutation rate of a newly generated chromosome

meta['local_search_iter'] = 4 # number of local search iterations
meta['step_size'] = 0.1 # step size for local search

meta['diff_search_iter'] = 4

# testing parameters
testing = {}
testing['take_previous'] = 0
testing['take_random'] = 0
# testing['limit_numbers'] = [0 for i in range(10)]

# -----------------------------------------------------------------------------
# preparations functions

# Prepare model
def prepare_model(a_rand_chrom):

  # Initialize chromosome and model
  model = GBNDM(a_rand_chrom)
  
  if args['cuda']:
      model.cuda()

  # Interpret learning rates and momentum
  lr1, lr2, lr2_epoch, a_momentum, lr_decr = interp_lrm(a_rand_chrom[0:4])
  train_params = (lr1, lr2, lr2_epoch, a_momentum, lr_decr)
  args['momentum'] = a_momentum
  if args['verbose_train']:
    print('lr1: {}'.format(lr1))
    print('lr2: {}'.format(lr2))
    print('lr2_epoch: {}'.format(lr2_epoch))
    print('a_momentum: {}'.format(a_momentum))
    print('a_decr: {}'.format(a_decr))

  return model, train_params


# -----------------------------------------------------------------------------
# stochastic hill-climbing algorithm functions

# Evaluate a list of chromosomes
def eval_chromosomes(list_chromosomes,num_chrom_params):
  global do_eval_iter, terminate_search
  
  # best_model_accur, best_chromosome, best_model, best_train_params = best_res
  
  best_model_accur = 0
  best_chromosome = None
  best_model = None
  best_train_params = None

  num_chrom = len(list_chromosomes)
  mat_chrom_acur = np.zeros((num_chrom, 1+num_chrom_params))
  neighb_valid_accurs = []

  for ci, a_chrom in enumerate(list_chromosomes):
    if do_eval_iter >= 250:
      terminate_search = True
      break
    if args['verbose_meta']:
      print('Chromosome {} ...'.format(ci))
    # --- Actual training
    model, train_params = prepare_model(a_chrom)
    model, valid_accurs, train_accurs = do_eval_chrom(model, train_params, args['num_epochs_search'])
    best_valid_accur = max(valid_accurs)
    # Store chromosome and accuracy
    mat_chrom_acur[ci,0] = best_valid_accur
    mat_chrom_acur[ci,1:] = a_chrom
    print('Best validation accuracy: {}%.'.format(best_valid_accur))
    neighb_valid_accurs.append(best_valid_accur)
    if best_valid_accur > best_model_accur:
      best_model_accur = best_valid_accur
      best_chromosome = a_chrom
      best_model = model
      best_train_params = train_params
      if args['verbose_meta']:
        print('*** Improving validation accuracy: {}.'.format(best_model_accur))
  
  best_res = (best_model_accur, best_chromosome, best_model, best_train_params)
  return best_res, mat_chrom_acur


# -----------------------------------------------------------------------------
# differential search functions

# differential function focused on perturbation

# Simple differential search v3
def do_diff_chrom_v3(mat_chrom_accur, num_new_sol, num_chrom_params):
  global best_res, diff_search_iter
  
  # Initialize new chromosomes
  num_chrom = mat_chrom_accur.shape[0]
  if num_new_sol >= num_chrom:
    num_new_sol = num_chrom-1
  new_chromosomes = []
  new_chromosomes.append(mat_chrom_accur[0].tolist())

  # Sort the array of chromosomes based on the first column (contains accur.)
  mat_chrom_accur = mat_chrom_accur[(-mat_chrom_accur[:,0]).argsort()]
  # Extract first/best chromosome
  best_chrom = mat_chrom_accur[0,1:]
  best_accur = mat_chrom_accur[0][0] / 100
  worst_accur = mat_chrom_accur[len(mat_chrom_accur)-1][0] / 100

  for dsi in range(1, len(mat_chrom_accur)):

    print("=========================================================")
    print("differential search iteration: {}".format(dsi))
    print("=========================================================")

    # 1. calculate difference of chromosomes params between best and ith chromosome
    ith_chrom = mat_chrom_accur[dsi, 1:]
    diff_chroms = best_chrom - ith_chrom
    # 2. generate scaling factor based on chromosome performance
    #   if accuracy_i > threshold, scale factor = -a
    #     perceived as close to a maximum, hence perturbate out to prevent clustering
    #   if accuracy_i < threshold, scale factor = 1-a
    #     perceived as far from a maximum, use current best as guidance to perturb into
    #   else accuracy_i = threshold, scale_factor = threshold
    #     perceived as can be improved, but scaling factor has to be small to prevent
    #     accidental perturbation away from a potentially closeby maximum
    #     a good heuristic is to take the threshold as the scaling factor

    #   if difference_acc > threshold, scale factor = 1-a
    #     perceived as far from a maximum, use current best as guidance to perturb into
    #   if difference_acc < threshold, scale factor = -a
    #     perceived as close to a maximum, hence perturbate out to prevent clustering
    #   else difference_acc = threshold, scale_factor = threshold
    #     perceived as can be improved, but scaling factor has to be small to prevent
    #     accidental perturbation away from a potentially closeby maximum
    #     a good heuristic is to take the threshold as the scaling factor
    ith_accur = mat_chrom_accur[dsi][0] / 100
    diff_accur = best_accur - ith_accur

    # use equation to calculate scale factor:
    # (20+n)/(10+n)-1/(x+0.1n)
    # OR
    # (20+x)/(10+x)-1/(x+0.1x), x = diff_accur
    # scale_factor = 2 - 1/diff_accur
    # scale_factor = 20.5/10.5 - 1/(diff_accur + 0.05)
    # scale_factor = 21/11 - 1/(diff_accur + 0.1)
    # scale_factor = 21.5/11.5 - 1/(diff_accur + 0.15)
    scale_factor = 11/6 - 1/(diff_accur + 0.2)
    # scale_factor = 12/7 - 1/(diff_accur + 0.4)
    # if diff_accur <= 0.00000000001:
    #   scale_factor = (20+0.00000000001)/(10+0.00000000001) - 1/(0.00000000001 + 0.1*0.00000000001)
    # else:
    #   scale_factor = (20+diff_accur)/(10+diff_accur) - 1/(diff_accur + 0.1*diff_accur)

    # threshold = (best_accur - worst_accur)/2 # 0.2 # 0.5 # 0.4 # 0.6

    # # use difference between best accuracy and ith accuracy
    # if diff_accur >= threshold:
    #   scale_factor = diff_accur
    # else:
    #   scale_factor = 1 / diff_accur

    # # directly use chromosome's accuracy
    # if ith_accur > threshold:
    #   scale_factor = 0 - ith_accur
    #   # scale_factor = ith_accur - 1
    # elif ith_accur < threshold:
    #   scale_factor = 1 - ith_accur
    # else:
    #   scale_factor = threshold

    # 3. directed vector generated by scaling difference of chromosomes by scaling factor
    directed_vec = diff_chroms * scale_factor
    # 4. add directed vector to ith chromosome to obtain a new chromosome
    a_new_chrom = ith_chrom + directed_vec
    np.clip(a_new_chrom, 0, 0.99999999999, out=a_new_chrom)

    # # 1. calculate dimensional mean of best chromosome
    # best_mean = np.mean(best_chrom)
    # # 2. calculate dimensional mean of ith chromosome
    # ith_chrom = mat_chrom_accur[dsi, 1:]
    # ith_mean = np.mean(ith_chrom)
    # # 3. calculate difference between mean of best chromosome and mean of ith chromosome
    # diff_mean = best_mean - ith_mean
    # # 4. generate vector containing random units [0, 1]^

    # #   if accuracy_i > threshold, scale factor = -a
    # #     perceived as close to a maximum, hence perturbate out to prevent clustering
    # #   if accuracy_i < threshold, scale factor = 1-a
    # #     perceived as far from a maximum, use current best as guidance to perturb into
    # #   else accuracy_i = threshold, scale_factor = threshold
    # #     perceived as can be improved, but scaling factor has to be small to prevent
    # #     accidental perturbation away from a potentially closeby maximum
    # #     a good heuristic is to take the threshold as the scaling factor

    # #   if difference_acc > threshold, scale factor = 1-a
    # #     perceived as far from a maximum, use current best as guidance to perturb into
    # #   if difference_acc < threshold, scale factor = -a
    # #     perceived as close to a maximum, hence perturbate out to prevent clustering
    # #   else difference_acc = threshold, scale_factor = threshold
    # #     perceived as can be improved, but scaling factor has to be small to prevent
    # #     accidental perturbation away from a potentially closeby maximum
    # #     a good heuristic is to take the threshold as the scaling factor
    # ith_accur = mat_chrom_accur[dsi][0] / 100
    # best_accur = mat_chrom_accur[0][0] / 100
    # threshold = 0.3 # 0.5 # 0.4 # 0.6
    # # use difference between best accuracy and ith accuracy
    # diff_accur = best_accur - ith_accur
    # if diff_accur > threshold:
    #   scale_factor = 1 - ith_accur
    # elif diff_accur < threshold:
    #   scale_factor = 0 - ith_accur
    # else:
    #   scale_factor = threshold
    # # # directly use chromosome's accuracy
    # # if ith_accur > threshold:
    # #   scale_factor = 0 - ith_accur
    # #   # scale_factor = ith_accur - 1
    # # elif ith_accur < threshold:
    # #   scale_factor = 1 - ith_accur
    # # else:
    # #   scale_factor = threshold

    # # 5. directed vector generated by scaling difference of means by random vector^
    # # ^may or may not be needed depending on implementation, could also change depending on implementation
    # directed_vec = [diff_mean * scale_factor for i in range(num_chrom_params)]
    # # 6. add directed vector to ith chromosome to obtain a new chromosome
    # a_new_chrom = ith_chrom + directed_vec
    # np.clip(a_new_chrom, 0, 0.99999999999, out=a_new_chrom)

    # evaluate new chromosome
    new_res, new_chroms_list = eval_chromosomes([a_new_chrom], num_chrom_params)

    if new_res[0] > best_res[0]:
      best_res = new_res

    new_chromosomes.append(new_chroms_list[0].tolist())
    # # add new chromosome to next iteration if it is better than its previous version
    # # otherwise carry over the previous chromosome
    # if new_res[0] > mat_chrom_accur[dsi][0]:
    #   new_chromosomes.append(new_chroms_list[0].tolist())
    # else:
    #   new_chromosomes.append(mat_chrom_accur[dsi].tolist())
    
    diff_search_iter += 1

  return new_chromosomes

  # # Scan through new solutions
  # for si in range(num_new_sol):
  #   # find average between first few best chromosomes
  #   # find difference between best chromosome and the worst ones
  #   # divide the difference by the differential learning rate
  #   #  larger difference -> assumed as the generation has not plateau-ed yet (has not reached local optima)
  #   #  smaller difference -> assumed as the generation has reached local optima and needs a larger push to get out from it
  #   difference_of_chroms = best_chrom - mat_chrom_accur[len(mat_chrom_accur)-1-si, 1:]
  #   difference_of_chroms = [0.00000000001 if i == 0 else i for i in difference_of_chroms]
  #   difference_of_chroms = [(meta['diff_lr']/difference_of_chroms[i]) for i in range(num_chrom_params)]
  #   # print(difference_of_chroms)
  #   # if np.isinf(difference_of_chroms):
  #   #   while True: pass
    
  #   # Add differential whilst applying a learning rate
  #   a_new_chrom = best_chrom + difference_of_chroms
  #   # interpolate up to 3 standard deviations from mean to [0,1) and clip the outliers
  #   avg_param_value = np.mean(a_new_chrom)
  #   std_param_value = np.std(a_new_chrom)
  #   a_new_chrom = np.interp(a_new_chrom, [avg_param_value - (3 * std_param_value), avg_param_value + (3 * std_param_value)], [0, 1])
  #   np.clip(a_new_chrom, 0, 0.99999999999, out=a_new_chrom)
  #   # Store new solution 
  #   new_chromosomes.append(a_new_chrom)

  # return new_chromosomes

def differential_search_v1(a_chrom, good_solutions, bad_solutions, num_chrom_params):
  global best_res, thresh_increase_rate

  # print("good sol len: {}".format(len(good_solutions)))
  # print("bad sol len: {}".format(len(bad_solutions)))
  if len(good_solutions) == 0 and len(bad_solutions) == 0:
    return a_chrom

  new_chromosomes = []

  if len(good_solutions) == 0:
    solutions_list = np.array(bad_solutions)
  elif len(bad_solutions) == 0:
    solutions_list = np.array(good_solutions)
  else:
    solutions_list = np.concatenate((good_solutions, bad_solutions))
  random.shuffle(solutions_list)
  while True:
    rand_chrom1 = random.randint(0, len(solutions_list)-1)
    rand_chrom2 = random.randint(0, len(solutions_list)-1)
    if rand_chrom1 != rand_chrom2:
      break
  rand_chrom1 = solutions_list[rand_chrom1]
  rand_chrom2 = solutions_list[rand_chrom2]
  
  dsi = 0
  for dsi in range(meta['diff_search_iter']):

    print("=========================================================")
    print("differential search iteration: {}".format(dsi))
    print("=========================================================")

    difference_accur = (rand_chrom1[0] - rand_chrom2[0]) / 100
    difference_vec = np.subtract(rand_chrom1[1:], rand_chrom2[1:])

    directed_vec = np.multiply(difference_vec, difference_accur)
    new_chrom = np.add(a_chrom[1:], directed_vec)
    
    # np.clip(new_chrom, 0, 0.99999999999, out=new_chrom)

    # damping trajectory
    for k in range(len(new_chrom)):
      if new_chrom[k] == 1.0:
        new_chrom[k] = 0.99999999999
      elif new_chrom[k] == 0.0:
        new_chrom[k] = 0.00000000001
      elif new_chrom[k] > 1.0:
        new_chrom[k] = new_chrom[k] - directed_vec[k]
        adjustment = (1.0 - new_chrom[k]) * 0.5
        new_chrom[k] += adjustment
      elif new_chrom[k] < 0.0:
        new_chrom[k] = new_chrom[k] - directed_vec[k]
        adjustment = new_chrom[k] * 0.5
        new_chrom[k] -= adjustment

    new_res, new_chrom_accur = eval_chromosomes([new_chrom], num_chrom_params)
    if new_res[0] > best_res[0]:# and random.random() < 0.8:  # 30% to not replace current best with new best to avoid overfitting
      thresh_increase_rate += (new_res[0] - best_res[0])/5
      best_res = new_res

    new_chromosomes.append(new_chrom_accur[0].tolist())
  
  return max(new_chromosomes)


def differential_search_v2(chroms_list, good_solutions, bad_solutions, num_chrom_params):
  global best_res

  new_chromosomes = []
  dsi = 0
  for dsi in range(len(chroms_list)):

    print("=========================================================")
    print("differential search iteration: {}".format(dsi))
    print("=========================================================")

    good_sol = random.choice(good_solutions)
    bad_sol = random.choice(bad_solutions)
    chrom = random.choice(chroms_list)

    difference_accur = (good_sol[0] - bad_sol[0]) / 100
    difference_vec = np.subtract(good_sol[1:], bad_sol[1:])

    directed_vec = np.multiply(difference_vec, difference_accur)
    new_chrom = np.add(chrom[1:], directed_vec)
    
    np.clip(new_chrom, 0, 0.99999999999, out=new_chrom)

    new_res, new_chrom_accur = eval_chromosomes([new_chrom], num_chrom_params)
    if new_res[0] > best_res[0]:# and random.random() < 0.8:  # 30% to not replace current best with new best to avoid overfitting
      best_res = new_res
    
    new_chromosomes.append(new_chrom_accur[0].tolist())

  sorted_new_chroms = sorted(new_chromosomes, reverse=True)
  for a_chrom in sorted_new_chroms:
    if a_chrom[0] > min(chroms_list.tolist())[0]:# and random.random() < 1 - (min(chroms_list)[0]/100):
      chroms_list[chroms_list.tolist().index(min(chroms_list.tolist()))] = a_chrom

  # considering changing to have elimination / production
  return chroms_list


# -----------------------------------------------------------------------------
# local search functions

def local_search(a_chrom, prev_chrom, num_chrom_params, pop_search_iter):
  global best_res, thresh_increase_rate, local_search_iter, testing

  ####################################################################
  # ALGORITHM TO IMPLEMENT
  ####################################################################
  # local search with random walking:
  # params: current chromosome, previous chromosome, number of chromosome params
  #   obtain trajectory from difference between current chromosome and previous chromosome
  #   scale trajectory down, this will be the starting direction in which to walk
  #   accuracy will determine probability of going in roughly the trajectory direction
  #   1-accuracy will determine probability of going other places
  #   evaluate chromosome when step taken
  #   update accuracy to new one

  # initial_chrom = a_chrom
  best_chrom = a_chrom

  current_chrom = a_chrom
  previous_chrom = prev_chrom

  max_iterations = math.floor(pop_search_iter * 0.2) + 3 # max number of iterations to prevent algorithm being stuck in local search
  breakout_countdown = 10 - math.ceil(best_chrom[0]/10)  ##### can change  # 10 - math.ceil(initial_chrom[0]/10) # larger values get less iterations  # 10 - math.ceil(best_chrom[0]/10) # larger values get less iterations
  lsi = 0
  reverse_direction = False
  while breakout_countdown >= 0 and max_iterations >= 0:

    print("=========================================================")
    print("local search iteration: {}".format(lsi))
    print("=========================================================")

    # get difference in accuracy between current chrom and previous chrom
    # get the threshold angle by (90deg * (1-abs(diff))) * current_accur
    # calculate initial trajectory by difference between current chrom and previous chrom
    # multiply trajectory by difference
    # generate random directed vector
    # compare angle between the vector and the current trajectory against the threshold angle
    # (diff = POSITIVE CASE)
    # if smaller, then
    #   add the vector to current chromosome
    #   update trajectory to new trajectory
    # otherwise
    #   add current trajectory to current chromosome
    # (diff = NEGATIVE CASE)
    # if smaller, then
    #   multiply vector by -1
    #   add the vector to current chromosome
    #   update trajectory to new trajectory
    # otherwise
    #   multiply trajectory by -1
    #   add current trajectory to current chromosome
    # evaluate new chromosome
    # get difference in accuracy

    accuracy = current_chrom[0] / 100
    accuracy_diff = (current_chrom[0] - previous_chrom[0]) / 100
    if accuracy_diff < 0:
      reverse_direction = True
      accuracy_diff = abs(accuracy_diff)
    threshold_angle = math.pi/2 * (1 - accuracy_diff) * accuracy

    trajectory = np.subtract(current_chrom[1:], previous_chrom[1:])
    trajectory = trajectory * accuracy_diff
    rand_vec = np.random.rand(num_chrom_param) * 0.1
    rand_vec = rand_vec + trajectory

    # calculate the unit vector for original trajectory and the randomized vector
    # source: https://stackoverflow.com/a/2827475
    unit_vec_trajectory = trajectory / np.linalg.norm(trajectory)
    unit_vec_rand = rand_vec / np.linalg.norm(rand_vec)
    # calculate the angle between the two vectors
    angle_between_vec = np.arccos(np.clip(np.dot(unit_vec_trajectory, unit_vec_rand), -1.0, 1.0))

    if not reverse_direction:
      if angle_between_vec <= threshold_angle:
      # if their angle is within the threshold angle, then the randomized vector is accepted
        testing['take_random'] += 1
        next_step = rand_vec
        # next_step = np.multiply(rand_vec, magnitude_ratio)
      else:
        # otherwise after some time, the default trajectory will be used
        testing['take_previous'] += 1
        next_step = trajectory
    else:
      if angle_between_vec > threshold_angle:
        # only if their angle is within the threshold angle, then the randomized vector is accepted
        testing['take_random'] += 1
        next_step = rand_vec * -1
        # next_step = np.multiply(rand_vec, magnitude_ratio)
      else:
        # otherwise after some time, the default trajectory will be used
        testing['take_previous'] += 1
        trajectory = trajectory * -1
        next_step = trajectory
    
    new_chrom = current_chrom[1:] + next_step
    
    # damping trajectory
    for k in range(len(new_chrom)):
      if new_chrom[k] == 1.0:
        new_chrom[k] = 0.99999999999
      elif new_chrom[k] == 0.0:
        new_chrom[k] = 0.00000000001
      elif new_chrom[k] > 1.0:
        new_chrom[k] = new_chrom[k] - next_step[k]
        adjustment = (1.0 - new_chrom[k]) * 0.5
        new_chrom[k] += adjustment
      elif new_chrom[k] < 0.0:
        new_chrom[k] = new_chrom[k] - next_step[k]
        adjustment = new_chrom[k] * 0.5
        new_chrom[k] -= adjustment
    
    new_res, new_chrom_accur = eval_chromosomes([new_chrom], num_chrom_params)
    if new_res[0] > best_res[0]:# and random.random() < 0.8:  # 30% to not replace current best with new best to avoid overfitting
      thresh_increase_rate += (new_res[0] - best_res[0])/5
      best_res = new_res

    # if the newly evaluated chromosome performs better than the currect best chromosome
    if new_chrom_accur[0][0] > best_chrom[0]:
      best_chrom = new_chrom_accur[0]
      breakout_countdown = 10 - math.ceil(best_chrom[0]/10)  ##### can change  # 10 - math.ceil(initial_chrom[0]/10) # larger values get less iterations  # 10 - math.ceil(best_chrom[0]/10) # larger values get less iterations
    else:
      breakout_countdown -= 1
    
    # if new_chrom_accur[0][0] <= initial_chrom[0]: ##### may change to best_accur
    #   breakout_countdown -= 1
    # else:
    #   breakout_countdown = 5  ##### can change  # 10 - math.ceil(initial_chrom[0]/10) # larger values get less iterations  # 10 - math.ceil(best_chrom[0]/10) # larger values get less iterations

    max_iterations -= 1
    previous_chrom = current_chrom
    current_chrom = new_chrom_accur[0]
    trajectory = next_step
    reverse_direction = False
    lsi += 1




    # trajectory = np.subtract(a_chrom[1:], prev_chrom[1:])
    # step = trajectory * 0.1
    # step_magnitude = sum(p**2 for p in step) ** .5  # (2)
    # magnitude_ratio = step_magnitude / sum(1 for p in range(num_chrom_params)) ** .5  # (2)
    # threshold_angle = math.pi/4 * accuracy + math.pi/4  # 45deg * accuracy + 45deg  # (2) # math.pi/2 * accuracy  # 90deg * accuracy
    # limit = 10
    # rand_vec = np.array([])
    
    # # # the higher the accuracy change, the more likely it will follow in the trajectory
    # # if random.random() < accuracy:
    # #   # maintain trajectory
    # #   new_chrom = np.add(a_chrom[1:], step) # (1)
    # # else:
    # #   # change trajectory
    # #   step = np.multiply(step, -1)          # (1)
    # #   new_chrom = np.add(a_chrom[1:], step) # (1)


    # # the higher the accuracy change, the more likely it will follow in the trajectory  # (2)
    # if random.random() < accuracy:
    #   # get threshold angle in radian by math.pi / 2 * accuracy (90 deg / accuracy)
    #   # generate random vector
    #   # check if angle is within threshold angle, if not repeat until it is obtained
    #   # limit the magnitude of the vector by the magnitude of the trajectory * 0.1
    #   # add the vector to a_chrom

    #   # generate a random vector to be the new trajectory
    #   rand_vec = gen_rand_chromosome(num_chrom_param) * 0.1
    #   rand_vec = np.add(rand_vec, trajectory)
    #   # calculate the unit vector for original trajectory and the randomized vector
    #   # source: https://stackoverflow.com/a/2827475
    #   unit_vec_trajectory = trajectory / np.linalg.norm(trajectory)
    #   unit_vec_rand = rand_vec / np.linalg.norm(rand_vec)
    #   # calculate the angle between the two vectors
    #   angle_between_vec = np.arccos(np.clip(np.dot(unit_vec_trajectory, unit_vec_rand), -1.0, 1.0))
    #   if angle_between_vec <= threshold_angle:
    #   # if their angle is within the threshold angle, then the randomized vector is accepted
    #     testing['take_random'] += 1
    #     next_step = np.multiply(rand_vec, magnitude_ratio)
    #   else:
    #     # otherwise after some time, the default trajectory will be used
    #     testing['take_previous'] += 1
    #     next_step = step
        
    #   new_chrom = np.add(a_chrom[1:], next_step)
    # else:
    #   # check if angle is outside of threshold angle, if not repeat until it is obtained
    #   # limit the magnitude of the vector by the magnitude of the trajectory * 0.1
    #   # add the vector to a_chrom

    #   # generate a random vector to be the new trajectory
    #   rand_vec = gen_rand_chromosome(num_chrom_param) * 0.1
    #   rand_vec = np.add(rand_vec, trajectory)
    #   # calculate the unit vector for original trajectory and the randomized vector
    #   # source: https://stackoverflow.com/a/2827475
    #   unit_vec_trajectory = trajectory / np.linalg.norm(trajectory)
    #   unit_vec_rand = rand_vec / np.linalg.norm(rand_vec)
    #   # calculate the angle between the two vectors
    #   angle_between_vec = np.arccos(np.clip(np.dot(unit_vec_trajectory, unit_vec_rand), -1.0, 1.0))
    #   if angle_between_vec > threshold_angle:
    #     # only if their angle is within the threshold angle, then the randomized vector is accepted
    #     testing['take_random'] += 1
    #     next_step = np.multiply(rand_vec, magnitude_ratio)
    #   else:
    #     # otherwise after some time, the default trajectory will be used
    #     testing['take_previous'] += 1
    #     next_step = np.multiply(step, -1)
        
    #   new_chrom = np.add(a_chrom[1:], next_step)
    
    # # damping trajectory
    # for k in range(len(new_chrom)):
    #   if new_chrom[k] == 1.0:
    #     new_chrom[k] = 0.99999999999
    #   elif new_chrom[k] == 0.0:
    #     new_chrom[k] = 0.00000000001
    #   elif new_chrom[k] > 1.0:
    #     new_chrom[k] = new_chrom[k] - next_step[k]
    #     adjustment = (1.0 - new_chrom[k]) * 0.5
    #     new_chrom[k] += adjustment
    #   elif new_chrom[k] < 0.0:
    #     new_chrom[k] = new_chrom[k] - next_step[k]
    #     adjustment = new_chrom[k] * 0.5
    #     new_chrom[k] -= adjustment
    
    # new_res, new_chrom_accur = eval_chromosomes([new_chrom], num_chrom_params)
    # if new_res[0] > best_res[0]:# and random.random() < 0.8:  # 30% to not replace current best with new best to avoid overfitting
    #   best_res = new_res
    
    # # if the newly evaluated chromosome performs better than the currect best chromosome
    # if new_chrom_accur[0][0] > best_chrom[0]:
    #   best_chrom = new_chrom_accur[0]
    #   breakout_countdown = 10 - math.ceil(best_chrom[0]/10)  ##### can change  # 10 - math.ceil(initial_chrom[0]/10) # larger values get less iterations  # 10 - math.ceil(best_chrom[0]/10) # larger values get less iterations
    # else:
    #   breakout_countdown -= 1
    
    # # if new_chrom_accur[0][0] <= initial_chrom[0]: ##### may change to best_accur
    # #   breakout_countdown -= 1
    # # else:
    # #   breakout_countdown = 5  ##### can change  # 10 - math.ceil(initial_chrom[0]/10) # larger values get less iterations  # 10 - math.ceil(best_chrom[0]/10) # larger values get less iterations

    # max_iterations -= 1
    # accuracy = new_chrom_accur[0][0] / 100
    # a_chrom = new_chrom_accur[0]

    # lsi += 1
    # limit = 10

  return best_chrom

  # # local search:
  # # current chromosome vector, add step (learning rate) and momentum ([-1, 1])
  # # 1. initialize step
  # # 2. randomly select momentum
  # # 3. multiply step and momentum
  # # 4. add to current chromosome vector
  # # 5. evaluate new chromosome with new chromosome vector
  # # 6. if decrease in accuracy^
  # #     - move in other direction*
  # #    else
  # #     - decrease magnitude of momentum and move in same direction
  # #     - have a chance for perturbing
  # # 7. repeat step 3 to 6 until termination criteria reached
  # #
  # # ^next step would be to change momentum with weight with repect to accuracy
  # # *depending on circumstances will have to do different things
  # step_size = meta['step_size']
  # momentum = []
  # for i in range(num_chrom_params):
  #   if random.random() < 0.5:
  #     momentum.append(-1.0)
  #   else:
  #     momentum.append(1.0)
  
  # # keeps a track record of the accuracies during local search
  # acur_record = [a_chrom[0]]
  # acur_ind = 0
  
  # chromosome = np.array(a_chrom[1:])
  # chrom_result = a_chrom
  # for lsi in range(meta["local_search_iter"]):

  #   print("=========================================================")
  #   print("local search iteration: {}".format(lsi))
  #   print("=========================================================")

  #   # convert to numpy arrays
  #   momentum = np.array(momentum)

  #   # generate movement vector as the mutation vector for local search
  #   movement_vec = momentum * step_size
  #   # add the movement to the chromosome
  #   chromosome = np.add(chromosome, movement_vec)
  #   np.clip(chromosome, 0, 0.99999999999, out=chromosome)
  #   # Test validation accuracy of new chromosome
  #   new_res, new_chrom_acur = eval_chromosomes([chromosome], num_chrom_params)
  #   acur_record.append(new_chrom_acur[0][0])
  #   acur_ind += 1

  #   if new_res[0] > best_res[0]:
  #     best_res = new_res
    
  #   if new_res[0] > chrom_result[0]:
  #     chrom_result = np.concatenate(([new_res[0]], new_res[1]))

  #   if acur_record[acur_ind] > acur_record[acur_ind - 1]:
  #     # if current accuracy is better than previous accuracy
  #     if acur_ind - 2 >= 0:
  #       if acur_record[acur_ind] - acur_record[acur_ind - 1] > acur_record[acur_ind - 1] - acur_record[acur_ind - 2]:
  #         # if current iteration's accuracy improvement is better than previous iteration's
  #         momentum *= 1.1
  #       else:
  #         # if current iteration's accuracy improvement is worse than previous iteration's
  #         momentum *= 0.8
  #     # else, retain momentum
  #   else:
  #     # if current accuracy is worse than or equal to previous accuracy
  #     if acur_ind - 2 >= 0:
  #       if acur_record[acur_ind] - acur_record[acur_ind - 1] > acur_record[acur_ind - 1] - acur_record[acur_ind - 2]:
  #         # if current iteration's accuracy regression is less than previous iteration's
  #         momentum *= 1.1
  #       else:
  #         # if current iteration's accuracy regression is more than previous iteration's
  #         momentum *= -0.8
  #     else:
  #       # else, go in opposite direction
  #       momentum *= -1.0
      
  #     local_search_iter += 1

  # return chrom_result # np.concatenate(([best_res[0]], best_res[1]))


# -----------------------------------------------------------------------------
# final evaluation functions

def final_test(a_model):
  a_model.eval()
  test_loss = 0
  correct = 0
  test_preds = torch.zeros(0)
  first = True
  for test_data_in, test_data_out in test_loader: 

      if args['cuda']:
          test_data_in, test_data_out = test_data_in.cuda(), test_data_out.cuda()
              
      test_data_in, test_data_out = Variable(test_data_in), Variable(test_data_out)
      output = a_model(test_data_in)
      test_loss += F.nll_loss(output, test_data_out, reduction='sum').data # sum up batch loss

      pred = output.data.max(1, keepdim=True)[1] # get the index of the max log-probability
      if first:
        test_preds = pred
        first = False
      else:
        test_preds = torch.cat((test_preds, pred),0)

      correct += pred.eq(test_data_out.data.view_as(pred)).long().cpu().sum()

  # Print test accuracy
  test_loss /= len(test_loader.dataset)
  accuracy = 100. * correct / len(test_loader.dataset)
  print('\nTest set: Average loss: {:.4f}, Accuracy (at final epoch): {}/{} ({:.0f}%)\n'.format(
      test_loss, correct, len(test_loader.dataset), accuracy))

  return accuracy, test_loss


# -----------------------------------------------------------------------------------------------------------
# start of algorithm

starting_time = time.time() # starting time to keep track of the total time taken to complete
all_accur_valid = []  # data of validation accuracy of each trial is stored here
all_accur_final = []  # data of final accuracy of each trial is store here
all_average_loss = [[],[]] # data if average loss if both validation accuracy and final accuracy are stored here

best_models_list = []
best_chromosomes_list = []
best_train_params_list = []

trials = 1 # 20
for ti in range(trials):
  meta_rs_valids = []
  best_model = None
  best_chromosome = None
  best_model_accur = 0

  do_eval_iter = 0
  local_search_iter = 0
  diff_search_iter = 0

  # list to store all initial search generated chromosomes
  mat_chroms = [[0 for x in range(comp_num_chrom_param(limits)+1)] for y in range(meta['max_rs_iter'])]


  # ---------------------------------------------------------------------------------------------------------
  # initial search

  # Start with a small search
  print('Initial random search ...')
  # best_chroms_sofar = [[1.0, 0.0042585, 0.96821215, 0.79260941, 0.60814645, 0.6, 0.7, 0.04, 0.5862289980281173, 0.7338889406769818, 0.9831628386889908, 0.1, 0.22289683015572181, 0.03963683957098263, 0.8857226147172315, 0.9907105206267093, 0.5961332999367686, 0.1, 0.18508997998966148, 0.4477437393500594, 0.38987942413825993, 0.7564195923109096, 0.6599304822448288, 0.1, 0.22264472171878957, 0.5910464532517522, 0.10596033472321698, 0.6836071794567388, 0.1675715431506537, 0.1, 0.8432675207672339, 0.34435815715024265, 0.05066839525588984, 0.06429038816402932, 0.8307779835894596, 0.0, 0.75, 0.9, 0.25, 0.9388082211746558, 0.5136862220847352, 0.22092548716659055, 0.1, 0.19661744162905903, 0.23397743491807688, 0.8562129181869463, 0.2819999033911843, 0.5045878564452496, 0.1, 0.04949089732105061, 0.8589185545712817, 0.4619181547389539, 0.12920291299592668, 0.4088703116572976, 0.1, 0.3936876666259238, 0.06386173698902409, 0.1626112003879724, 0.6863630243718888, 0.20484341337972356, 0.1, 0.2899307065500917, 0.3474828774864761, 0.31483248523124563, 0.4699034702614928, 0.6295018634035641, 0.0, 0.75, 0.9, 0.04, 0.4865504330457404, 0.7419913126058634, 0.6259912582937586, 0.1, 0.7521338724139414, 0.7591530760699826, 0.2601862616459406, 0.18439681137560304, 0.13203008922873294, 0.1, 0.8673993609453339, 0.11391513895558747, 0.9580254899314892, 0.5525916001722413, 0.32094021430021213, 0.1, 0.9423310161325864, 0.19563376820647194, 0.510299252556756, 0.7221582766963771, 0.5007543199225671, 0.1, 0.2849826296458532, 0.2308831399911928, 0.19148862459256577, 0.23234228388782963, 0.9252620241361028, 0.0, 0.75, 0.9, 0.04, 0.1161038252714679, 0.4100523402498414, 0.20909254753366857, 0.1, 0.368063931314179, 0.33659228124857854, 0.7265167874563633, 0.9296306490905453, 0.08051960378737999, 0.1, 0.21115612020231644, 0.5860739280631799, 0.3747853365579902, 0.06866732372508699, 0.4373587990980389, 0.1, 0.1774803041246812, 0.19691553122067174, 0.18669555283578854, 0.16848284766705535, 0.6554840388810601, 0.1, 0.6612725642480186, 0.536019642209252, 0.6361506023033575, 0.5908274812208694, 0.6486447622317318, 0.0, 0.75, 0.49, 0.04, 0.6387036244425293, 0.40887539425665154, 0.011454148054169222, 0.1, 0.03585295632485741, 0.6298636982341455, 0.6756109736878301, 0.4491281751417866, 0.5038171132896895, 0.1, 0.6101911588520668, 0.29657731223804773, 0.04521282306419461, 0.444501386865481, 0.6699783800181469, 0.1, 0.0032545549711846533, 0.9002369615097993, 0.9895873397577304, 0.07127605525629654, 0.8664995606515336, 0.1, 0.7371783478655493, 0.634167477303578, 0.6237529608637132, 0.45830461121267907, 0.5932173619259021, 0.0, 0.75, 0.49, 0.04, 0.37681165971649144, 0.9822652581085561, 0.7025539278491114, 0.1, 0.39401649107802184, 0.6521254217538357, 0.5699368578245115, 0.9869678887568852, 0.7155267579296767, 0.1, 0.8774692262281457, 0.7706132049120323, 0.8393667033078814, 0.3627046369722807, 0.6021822713359493, 0.1, 0.8829491096405716, 0.9678887126747475, 0.4599559791290597, 0.39023431921745044, 0.10118296512841263, 0.1, 0.3569650999005046, 0.8535276107771494, 0.8162676025497952, 0.052520108525119014, 0.46315884048829803, 0.0, 0.75, 0.49, 0.04, 0.1106441484020324, 0.22166365173992242, 0.7537615847436709, 0.1, 0.4567944154933584, 0.5729342056888236, 0.7748035103829326, 0.2813026536541382, 0.0493726001824244, 0.1, 0.16700954934569623, 0.9887321725151681, 0.43652001995788936, 0.07133629227959193, 0.9155566900459361, 0.1, 0.42150068730753887, 0.579991551991645, 0.7571339557072196, 0.8727741595624557, 0.3813585186037143, 0.1, 0.653618532249587, 0.3392814845660058, 0.7182376749155149, 0.22663073994604543, 0.8971090554232307, 0.0, 0.9, 0.9, 0.15, 0.6731169797303737, 0.5868653358624377, 0.6109431497568409, 0.1, 0.10796936098940346, 0.324698524918963, 0.6398047637966162, 0.9468515617700778, 0.8547403413163267, 0.1, 0.6728955063294789, 0.5284662052547354, 0.9568627728062639, 0.28982647655966276, 0.20540940444384725, 0.1, 0.6417019649576262, 0.5965359095759659, 0.208186464820722, 0.6030632429267482, 0.1220794310774499, 0.1, 0.9729472751221221, 0.41085950588183084, 0.8787047875141635, 0.3009438653503239, 0.20935761660011865, 0.0, 0.75, 0.49, 0.04, 0.5516991588796037, 0.2882500962436598, 0.5354120772589978, 0.1, 0.12244930891466244, 0.12956496393398642, 0.9969589658896316, 0.3404096644052884, 0.8648795532427661, 0.1, 0.5927117524336191, 0.15914160367977104, 0.6863668446182342, 0.766845862687294, 0.6965958976441318, 0.1, 0.8789368115813703, 0.7283163664917722, 0.4047138398606315, 0.35779287024362727, 0.6818937188092669, 0.1, 0.9795070644279046, 0.6220100273801911, 0.07186842439061036, 0.41943046905558734, 0.1646665009749274, 0.7, 0.1, 0.6975627326331982, 0.18339278410720117, 0.41800720030549243, 0.5695312087966132, 0.4382256822805616, 0.1, 0.8761973981354831, 0.5390652637340876, 0.21629796216094266, 0.5619807545663222, 0.44259448151457637, 0.1, 0.030890999887054016, 0.6395351671654926, 0.590727614666816, 0.6856344431349125, 0.36104351900147547, 0.1, 0.8446450863641146, 0.7261690780714333, 0.5064306492386194, 0.9462380237425082, 0.6095160345501639, 0.1, 0.9956996406999898, 0.577087529144052, 0.7715751031452773, 0.13310893151229375, 0.5888764382773022, 0.8, 0.1, 0.8194189156086866, 0.09772217297740216, 0.6277921694867977, 0.40163363872772606, 0.7289725857215003, 0.1, 0.5619386615051809, 0.9344841336938915, 0.05737855616757004, 0.2455196590077906, 0.32848590110990705, 0.1, 0.9087336204003725, 0.4870098993925547, 0.5715681304311075, 0.9685481753098858, 0.43246612110357574, 0.1, 0.7967976349448525, 0.9435970142221479, 0.6214504600923542, 0.08606789001074411, 0.3210160838883407, 0.1, 0.9356886266258222, 0.8850869903769888, 0.18228507851915043, 0.25930460154704515, 0.6641791684991025, 1.0, 0.75, 0.49, 0.04, 0.2784479746377059, 0.6921711199218177, 0.9831997893193135, 0.1, 0.6359667249559994, 0.29974395028421263, 0.2320856747309793, 0.3817538333445031, 0.43304281917826504, 0.1, 0.4705430751757502, 0.6986467424495189, 0.6871743948025942, 0.005461401702960611, 0.0820811343863801, 0.1, 0.055100757310700965, 0.13986973245594714, 0.5255579943494832, 0.6635554086741535, 0.30389438981126593, 0.1, 0.4339146121246821, 0.8801389333200129, 0.8669175868604352, 0.7514422536666452, 0.8104041354509818], [0.21588026394445314, 0.6848959796748076, 0.4114673282821517, 0.32087318961825395, 0.60814645, 0.6, 0.7, 0.04, 0.7385170915233299, 0.9492306789889069, 0.10806478142572218, 0.1, 0.40699294299473887, 0.220652751692479, 0.5221987431752454, 0.9531918148401214, 0.7108011052202553, 0.1, 0.3632471321172803, 0.9858569408409239, 0.7671944017880953, 0.39967060291336487, 0.18883702603841535, 0.1, 0.4284013855621551, 0.5841851614157096, 0.7018683651410561, 0.27169651432473707, 0.0864696193079878, 0.1, 0.9491822879920114, 0.09848018748769372, 0.950039508116608, 0.1847162066682827, 0.9382119603450761, 0.0, 0.75, 0.9, 0.25, 0.6075730332804294, 0.5388766057607799, 0.24494065804643483, 0.1, 0.6440253382091529, 0.19883133713690937, 0.1550298194695492, 0.8311273047031315, 0.7863026740916026, 0.1, 0.8424312882638578, 0.3490880118138414, 0.5000731821646572, 0.5289930705861967, 0.7282605120342808, 0.1, 0.5435644149465261, 0.9258863360521216, 0.43763059919813996, 0.4044794685717614, 0.7388385505139995, 0.1, 0.01177101685941806, 0.8236481934980598, 0.5007726365284245, 0.6456561262222829, 0.24936252959707617, 0.0, 0.75, 0.9, 0.04, 0.8350979837898299, 0.5172408965759411, 0.9270011724629087, 0.1, 0.6955843896803375, 0.8351774634097852, 0.4846472231443737, 0.6268688322717897, 0.2958644951241943, 0.1, 0.7538846298970917, 0.04775095930742557, 0.6841323657940478, 0.8067585209785416, 0.9034246327212648, 0.1, 0.3623537826616846, 0.29716493032592084, 0.2258524660522525, 0.7120006648114028, 0.9430601806541564, 0.1, 0.7546983953958348, 0.07465024183351521, 0.3615512634251754, 0.31651562914682985, 0.8112698427779907, 0.0, 0.75, 0.9, 0.04, 0.35883546830905744, 0.49928620285338765, 0.22416797995227877, 0.1, 0.8399290241330916, 0.8905934706487175, 0.6862531979522164, 0.655630857014207, 0.6889405323872587, 0.1, 0.24402942456429066, 0.8415831029711035, 0.3631588353926364, 0.9751439917201448, 0.34777531185905064, 0.1, 0.8060793559301055, 0.23858019703562594, 0.7071869270039598, 0.3948082176307367, 0.6608404053586948, 0.1, 0.2860089906038289, 0.6180772181064205, 0.17760532202808443, 0.5496411583404242, 0.6121154608080439, 0.0, 0.75, 0.49, 0.04, 0.8763482936984434, 0.36895671805321484, 0.7456762539578224, 0.1, 0.49482439474469275, 0.44702145144459215, 0.2308431445766721, 0.45797262613584366, 0.20713006623787256, 0.1, 0.19200345833582233, 0.6245382246282355, 0.8297947493452553, 0.00532698615894267, 0.05535573600860022, 0.1, 0.2848046840855536, 0.5654506094715793, 0.1612882579905125, 0.8278471662441397, 0.8753328027367449, 0.1, 0.09126335016254161, 0.937433658473994, 0.5253693040505838, 0.7796679310041567, 0.7895582113455228, 0.0, 0.75, 0.49, 0.04, 0.7955808528596133, 0.7300736794082702, 0.43061845927276454, 0.1, 0.38815576012256303, 0.28166488480602425, 0.29520907788714323, 0.23592800199964414, 0.36913349057230804, 0.1, 0.30899677178811924, 0.8057186347996828, 0.6591041947244841, 0.8355312442184433, 0.7406822628257611, 0.1, 0.1901518231589877, 0.6765074929108301, 0.7408422493332019, 0.8902973020324076, 0.3245873573254645, 0.1, 0.051863918680003285, 0.5828048554779229, 0.9291539426397217, 0.4646985684277607, 0.3379234447676609, 0.0, 0.75, 0.49, 0.04, 0.08608119646125367, 0.056082253141569005, 0.5710635718958212, 0.1, 0.5290425322212935, 0.8773728438592825, 0.2499877065163446, 0.9435406068767546, 0.3225595810303602, 0.1, 0.7015690221295047, 0.9801624816084916, 0.8704720830921714, 0.3462770671083729, 0.7797664565569411, 0.1, 0.8311092484717161, 0.35118279302933264, 0.12391592264875029, 0.8395517954917656, 0.8302852876445809, 0.1, 0.6470619437345639, 0.4031417896808758, 0.26522362573106284, 0.612394781838791, 0.3787794909846274, 0.0, 0.9, 0.9, 0.15, 0.6731169797303737, 0.9801760620369239, 0.7952460335469803, 0.1, 0.5492644132949362, 0.5400589082718068, 0.6142625896064311, 0.18888011935557547, 0.10193711489581792, 0.1, 0.7628858514290726, 0.09085357968853158, 0.5157742511137685, 0.34748277285992235, 0.7375662358339825, 0.1, 0.06748273078986977, 0.057101035730721894, 0.9417412656254122, 0.3296519685131848, 0.9625980793340568, 0.1, 0.3771909397391964, 0.13641763513689797, 0.8975550681544487, 0.1322446349835512, 0.5681579304257868, 0.0, 0.75, 0.49, 0.04, 0.5778598018841024, 0.6063324993436474, 0.9583919003950184, 0.1, 0.8659505323240698, 0.316516693287611, 0.5819090813808561, 0.1021765438050487, 0.33806560804361874, 0.1, 0.1681837363445453, 0.2095915941668487, 0.1742370771144638, 0.3305455208263002, 0.35745660413661295, 0.1, 0.3907028575794307, 0.639935443892715, 0.6645699275639353, 0.8610820808956127, 0.6871788395216971, 0.1, 0.1975343923173607, 0.10859757283077232, 0.6274995894521704, 0.8961564397920246, 0.4330545489590092, 0.7, 0.1, 0.12191030042882411, 0.11983206359448328, 0.20777851923004664, 0.010359308319917826, 0.360645018156204, 0.1, 0.2900861273665988, 0.7012774389686433, 0.6376400730222912, 0.8835455280857797, 0.31866814399030574, 0.1, 0.23526907755400328, 0.6379787495284238, 0.10586726671444335, 0.8432312706322107, 0.3792446267548206, 0.1, 0.7095527494374598, 0.1691984465158769, 0.3565820598259978, 0.9072444547769184, 0.9351361053896448, 0.1, 0.44480354309159054, 0.8952181593951437, 0.44676300389137547, 0.8887166915276541, 0.5880473920708167, 0.8, 0.1, 0.7711856527685861, 0.3603701443716053, 0.8316537295021889, 0.16630050295645504, 0.9926089194928208, 0.1, 0.4235111612859541, 0.6503820621684616, 0.6872917353474209, 0.6132113945198079, 0.2459585192275704, 0.1, 0.22438079087548768, 0.44501490671215826, 0.943242596240085, 0.2209424445446002, 0.8384894723312863, 0.1, 0.8880100199852489, 0.0221391673286353, 0.37620804101918615, 0.13679112440400387, 0.25962290756786144, 0.1, 0.19302187705511786, 0.09051469191926376, 0.5604065188739206, 0.5601227293132253, 0.7692922094355324, 1.0, 0.75, 0.49, 0.04, 0.14705893339694842, 0.03424992953939143, 0.056811716443358584, 0.1, 0.6691413230686074, 0.015793270720897867, 0.23876942984868643, 0.396300879385475, 0.8425481612109408, 0.1, 0.07862689609800988, 0.142163477373484, 0.2399627030408401, 0.12333554144422043, 0.5306947554606525, 0.1, 0.22179947305643233, 0.37792033372196887, 0.12490846278329015, 0.6870063226090286, 0.32789810033749744, 0.1, 0.568625765293772, 0.09294810036768542, 0.6962930436890328, 0.23710336542783594, 0.6960046275590863], [0.2747115574756054, 0.21729582929892233, 0.15954077319714832, 0.1512202807892169, 0.60814645, 0.6, 0.7, 0.04, 0.9342050319206766, 0.15745671160032237, 0.7643052571752685, 0.1, 0.42390941666878523, 0.9235057012524802, 0.655327611817392, 0.8725234869658234, 0.06633028828423504, 0.1, 0.48250599338594435, 0.49180938036843314, 0.2531060774302042, 0.009283406096449398, 0.04430759138736262, 0.1, 0.6594372254640859, 0.0006806343399373693, 0.30441929408160884, 0.6400989711788408, 0.8476790654904729, 0.1, 0.5599220089550062, 0.9061824924457471, 0.942309502698717, 0.8643768162166757, 0.5833861823575095, 0.0, 0.75, 0.9, 0.25, 0.2341110795028406, 0.8604751260990425, 0.6401063014907968, 0.1, 0.11718996042857222, 0.32730277332098223, 0.3105650835437084, 0.5217247526259537, 0.5732601617609826, 0.1, 0.5336032722015581, 0.45861467512008225, 0.590118232460013, 0.028908440132721758, 0.0865589573982426, 0.1, 0.30408387107887747, 0.848531652842136, 0.9924979622431012, 0.17717165136564306, 0.23237147755304866, 0.1, 0.9485025628232934, 0.3109919357669164, 0.7190887507327558, 0.8154319716549074, 0.4535390780987125, 0.0, 0.75, 0.9, 0.04, 0.5790653737550261, 0.01640172158921005, 0.3206865997529753, 0.1, 0.7110069392435121, 0.952608438550504, 0.3541218631581776, 0.4461106052500591, 0.9239450111031393, 0.1, 0.6424718025836694, 0.5605574738228939, 0.09545808155206525, 0.4500268361551846, 0.5758723636318674, 0.1, 0.35512016894237375, 0.23110236068745005, 0.8651743199813292, 0.6866072123507774, 0.580165813756166, 0.1, 0.8800792600184364, 0.7628512652300231, 0.10811441083541451, 0.3160767115873183, 0.6793798582153785, 0.0, 0.75, 0.9, 0.04, 0.6317677948599364, 0.4506783494211044, 0.6122291338988571, 0.1, 0.055370482971791146, 0.1983858533980295, 0.11154942673764545, 0.4499231904253391, 0.07297537071573357, 0.1, 0.9755846457529985, 0.09517735090009294, 0.7113062362352602, 0.7275123261955435, 0.7547374667191992, 0.1, 0.792911485477515, 0.29802246594193127, 0.9030704778333499, 0.9118306685852422, 0.7347345904505355, 0.1, 0.4439851203588986, 0.6555882648422887, 0.18252156747794024, 0.759390662278077, 0.8054077281368675, 0.0, 0.75, 0.49, 0.04, 0.37988645157828016, 0.9704107119162385, 0.21639884489466, 0.1, 0.29323003918231993, 0.6551598304824056, 0.05222070757837671, 0.14944560602223222, 0.4982815550188592, 0.1, 0.1389962720488076, 0.6173817060467142, 0.676140614710025, 0.476738176995363, 0.217286022294779, 0.1, 0.4093881411554827, 0.4740852343825721, 0.6923638321331979, 0.6783098964472746, 0.9381129337237356, 0.1, 0.531085940452148, 0.04240378896394292, 0.5797179547562065, 0.8806500205807472, 0.6636261857120497, 0.0, 0.75, 0.49, 0.04, 0.43734167340562335, 0.8948527976596892, 0.25347453621254523, 0.1, 0.65274979729833, 0.8673081066966049, 0.09882743187165777, 0.9591571087870203, 0.2080760243986396, 0.1, 0.4064072689898467, 0.9651774834741127, 0.465303188072577, 0.7217248418066926, 0.705142332184556, 0.1, 0.4561584752222392, 0.2397195518495805, 0.9050083596155702, 0.10855229948508593, 0.7318598181404683, 0.1, 0.9999027220518024, 0.49774731888328116, 0.5097393480112551, 0.7760863057801285, 0.6142051671317272, 0.0, 0.75, 0.49, 0.04, 0.7759040931024173, 0.08354526885811997, 0.44641515817140653, 0.1, 0.18331897580963274, 0.36234585602979386, 0.6798836281923265, 0.2313998742265022, 0.1111650058785475, 0.1, 0.7392898299894529, 0.9748053963277622, 0.9851086623426265, 0.18911066629503714, 0.3284575358411248, 0.1, 0.2700064522016039, 0.37874678666338546, 0.5654583240380283, 0.9936197108052133, 0.6267636777893819, 0.1, 0.6899878025269983, 0.5669146817398105, 0.6300376297808824, 0.21621504834132466, 0.23099511533667416, 0.0, 0.9, 0.9, 0.15, 0.6731169797303737, 0.5008462733352673, 0.7053323542421378, 0.1, 0.300216071001113, 0.7609172278707791, 0.14345353933654803, 0.8108076311949691, 0.5264215027844409, 0.1, 0.5281009942354575, 0.1339999092961931, 0.31656386404088266, 0.28110242862101575, 0.21054964199422843, 0.1, 0.813973037892938, 0.2204421828265598, 0.9013330380125997, 0.22603347696966958, 0.7812885853753088, 0.1, 0.871180690073399, 0.3352277749512639, 0.8574799041557446, 0.5867255715899502, 0.7115754922215186, 0.0, 0.75, 0.49, 0.04, 0.37224500385417547, 0.9907897843676773, 0.3296616759411706, 0.1, 0.8216956259931055, 0.5591168695046517, 0.9643528704193802, 0.6266301930790678, 0.6171211557849723, 0.1, 0.5091864802548398, 0.3302460145801407, 0.7547272442770793, 0.843819728807151, 0.24305519447445523, 0.1, 0.8838530270448555, 0.010513975308566348, 0.922172653136818, 0.9093684115925226, 0.5437101843900625, 0.1, 0.7592060569949357, 0.2705706187668737, 0.0964422736564644, 0.2935475296135973, 0.19583477452206288, 0.7, 0.1, 0.7672996024526199, 0.2893595527953523, 0.6182660035914055, 0.08851855369833517, 0.04606617117643952, 0.1, 0.984331159916342, 0.6833951130586141, 0.05070205098586933, 0.08260205691122813, 0.9442041691651207, 0.1, 0.7391043417478068, 0.5933081137847894, 0.2848079298039201, 0.2800667947832629, 0.7831735762022952, 0.1, 0.4386537890990422, 0.21965709308041925, 0.1199282761154985, 0.44959662368148257, 0.5872601294609722, 0.1, 0.5992652877412654, 0.8398835801529334, 0.9128307638987975, 0.3667238324171723, 0.9743204271635358, 0.8, 0.1, 0.3198496947089098, 0.5878038439443182, 0.9425995950617034, 0.7037940630965874, 0.9630891740057174, 0.1, 0.16338705226558048, 0.6866906749232452, 0.7347150743506934, 0.6812786289509921, 0.4324658267017736, 0.1, 0.6232826540371827, 0.2366498189751539, 0.6937804133281992, 0.09126245788883369, 0.10684701765842808, 0.1, 0.3133595457606969, 0.30396932385642805, 0.5165160909615386, 0.15748437171842466, 0.18739650174973133, 0.1, 0.7386447758570693, 0.8989460895514735, 0.3363689440559533, 0.0030493984467941937, 0.9370237714318036, 1.0, 0.75, 0.49, 0.04, 0.15937341694256812, 0.6536833587047114, 0.8109445335328221, 0.1, 0.8398144590129804, 0.30590966232714445, 0.7753075486692836, 0.6468237085781803, 0.9470507081947903, 0.1, 0.6548160583340653, 0.43631158355990673, 0.07573530039347931, 0.6924895576641453, 0.4860077505481104, 0.1, 0.5901648945710207, 0.17804463725633246, 0.20738021289598008, 0.50204199135158, 0.15532368802989116, 0.1, 0.4934833493808728, 0.09212561058237423, 0.41450688556397774, 0.16775031568040066, 0.35070348104165183], [0.8669344593422416, 0.6774542389666912, 0.317952685439447, 0.050022681273811154, 0.60814645, 0.6, 0.7, 0.04, 0.2501370857708969, 0.07281529325125558, 0.4139504925503914, 0.1, 0.13660378760744207, 0.7923928259762784, 0.3852426807478916, 0.394496627568671, 0.38780176647095843, 0.1, 0.8031635072218466, 0.822875902157939, 0.33023102682031313, 0.0500719136587916, 0.4603901571734035, 0.1, 0.6979053366927795, 0.38560668148564636, 0.9193186836694229, 0.9083671764733987, 0.007488531269828735, 0.1, 0.3603085588747451, 0.7436055833265273, 0.7458146573189659, 0.3540059098117939, 0.19417364128215042, 0.0, 0.75, 0.9, 0.25, 0.8531066503147821, 0.5858796199860177, 0.3726962633842652, 0.1, 0.2648007170569173, 0.47548747088621524, 0.3980166370140146, 0.010264351951452921, 0.5304582143756801, 0.1, 0.9381288016877858, 0.5846236086526908, 0.790016465396803, 0.9971452841395159, 0.02269985293633714, 0.1, 0.36300419109475124, 0.5663972845880719, 0.4291033452225903, 0.8413747643197064, 0.13478248245456237, 0.1, 0.31266666159603285, 0.9647462583909592, 0.064714883317193, 0.13644186779898138, 0.8319388659570266, 0.0, 0.75, 0.9, 0.04, 0.4751653215827726, 0.22257425401227227, 0.7823676002235729, 0.1, 0.8572513088434776, 0.5581482278938596, 0.17635882260432278, 0.9947443065401965, 0.8487112100942975, 0.1, 0.12346943746144934, 0.06598009157391016, 0.02222320631732544, 0.6725453998908113, 0.1517272819559271, 0.1, 0.23480750943510587, 0.11918386024266336, 0.7104759174578925, 0.02870935741557057, 0.8748040818028509, 0.1, 0.12060712904712811, 0.3164989919885428, 0.5218343200124486, 0.2965223661862505, 0.1763644785447388, 0.0, 0.75, 0.9, 0.04, 0.38755868482792355, 0.7113670951466708, 0.19183688136781618, 0.1, 0.08320285650303172, 0.9323669240183556, 0.4530683001438748, 0.6714722993978554, 0.9556160587614251, 0.1, 0.82024684885723, 0.675356041417061, 0.5767690567221732, 0.0613248611558167, 0.4808553860309994, 0.1, 0.3224114663127813, 0.4580890453611063, 0.7473781620389621, 0.8887941527104576, 0.689292404782689, 0.1, 0.8955425934273135, 0.9685618493895763, 0.1067350394099752, 0.26301216082897205, 0.9980340064623938, 0.0, 0.75, 0.49, 0.04, 0.3854704255168755, 0.9388641882609918, 0.21848138746086065, 0.1, 0.8890936784491189, 0.996280405286035, 0.5606327582306773, 0.15715722186780046, 0.9349560846811218, 0.1, 0.6256937181101486, 0.24171798717266657, 0.8878491294822609, 0.876985104812224, 0.06114330622641895, 0.1, 0.7670154408114287, 0.3757502850521486, 0.3328552501874822, 0.11742740391074502, 0.4090405512166644, 0.1, 0.9040436134875388, 0.3157823135206562, 0.40745102336614103, 0.2808534742475298, 0.010243473739881104, 0.0, 0.75, 0.49, 0.04, 0.504343895398777, 0.9565870265941074, 0.8125512592105663, 0.1, 0.5850197974445661, 0.938414581864677, 0.7128052099849422, 0.9828319764404357, 0.6350946759689842, 0.1, 0.5911800698522343, 0.35685300744010573, 0.5310475499064156, 0.4970734890683931, 0.2528884880675264, 0.1, 0.7276832562404046, 0.18764049319895182, 0.14607956948020395, 0.08980294923627752, 0.40254569588964584, 0.1, 0.6144589450720185, 0.5639476089598273, 0.45633349734206186, 0.6962510203325867, 0.9694269320621585, 0.0, 0.75, 0.49, 0.04, 0.5296778054623266, 0.555300079564294, 0.5631381025312061, 0.1, 0.8845114776782463, 0.6874644203027287, 0.6402338444687686, 0.6509856413042803, 0.9327923655943747, 0.1, 0.05047967950600707, 0.46768577170043446, 0.6278563981939047, 0.7309302354715785, 0.3085769122348654, 0.1, 0.9510439133303134, 0.8710676193900376, 0.4401559644258366, 0.4798463426031122, 0.6310155593121678, 0.1, 0.9797065511039252, 0.5972261558519404, 0.8483208632200532, 0.3123243465306228, 0.8174555222446109, 0.0, 0.9, 0.9, 0.15, 0.6731169797303737, 0.4923727418875531, 0.14996572593247348, 0.1, 0.5172592767688626, 0.40590225420058956, 0.5254306577474369, 0.3616556839502051, 0.5828878505617787, 0.1, 0.5594485651701239, 0.43061508191801645, 0.3715323938067794, 0.8891219165659555, 0.11893185290131669, 0.1, 0.4865876086378612, 0.15503565064613622, 0.5932628798200152, 0.30716546530085276, 0.7533335136753886, 0.1, 0.7955803132752616, 0.021434901167131315, 0.98108204353318, 0.023649447201608997, 0.3423837487407563, 0.0, 0.75, 0.49, 0.04, 0.7307340220353112, 0.724002868992059, 0.6653133486037379, 0.1, 0.1841956247686649, 0.9161635965087708, 0.36577263689919726, 0.801632854009658, 0.43858954943625783, 0.1, 0.449004608194989, 0.5934946598632934, 0.34459799084861475, 0.7216775858491798, 0.1982423902102004, 0.1, 0.5319339077741467, 0.6219949960838731, 0.06511482169844884, 0.5429324647500323, 0.21310245128350136, 0.1, 0.6134750057588755, 0.9719251546138474, 0.3250805883843594, 0.18174742310115266, 0.5460366682312495, 0.7, 0.1, 0.28145700579708055, 0.9995557909155122, 0.9876760259582847, 0.1800590256976451, 0.9151199936844016, 0.1, 0.5098076340489753, 0.4962712684316176, 0.1627601642996862, 0.33035602176707346, 0.5472324617746432, 0.1, 0.12830408304298413, 0.9880200170253781, 0.5886771338897131, 0.025688139879385785, 0.6758768393728773, 0.1, 0.8649753620569159, 0.7348602751709006, 0.34813287285357186, 0.7782037440383601, 0.6712301965344594, 0.1, 0.5288427050340196, 0.8752695762412336, 0.9401309835812365, 0.9302287549341874, 0.27121479799013726, 0.8, 0.1, 0.8300276318728361, 0.8685799903569156, 0.852191122836866, 0.8494155308475263, 0.2726907271878174, 0.1, 0.023423051560382158, 0.7556933925877215, 0.618960406745683, 0.5474700100079802, 0.5690313903639009, 0.1, 0.8488732213550088, 0.7312487105252485, 0.5025020930407116, 0.8805787141668417, 0.6051675701539213, 0.1, 0.4141559046974709, 0.2185297790896723, 0.8467647771602174, 0.4498325103266434, 0.36590061262660734, 0.1, 0.4126629536170847, 0.33930383321210245, 0.14591309624357485, 0.8557476121747035, 0.3749266341958488, 1.0, 0.75, 0.49, 0.04, 0.0948367642692377, 0.9635655483057695, 0.7558894215446965, 0.1, 0.6579850349912932, 0.3969800084873025, 0.11346166458310059, 0.2799177375230236, 0.5913522219787035, 0.1, 0.8794936650164887, 0.7866575446481981, 0.8008121681810653, 0.14996400280678746, 0.5129517176440915, 0.1, 0.3105050324473533, 0.6660298635228153, 0.21768484802897448, 0.20383825084207308, 0.500743078235376, 0.1, 0.40728266705168836, 0.9860223689219039, 0.6981585738556934, 0.6460294959357032, 0.9288562692224767], [0.7619766337998289, 0.14338617990627978, 0.7682155843481846, 0.48219473966386206, 0.60814645, 0.6, 0.7, 0.04, 0.8782929956629565, 0.7150952285830742, 0.8122261353862376, 0.1, 0.9842135130351776, 0.49280105653053907, 0.9305062463363655, 0.6696066988397074, 0.9357375951248119, 0.1, 0.5446103766994058, 0.8219583411815574, 0.004943505579708352, 0.1929898577450131, 0.7600080706490141, 0.1, 0.4195242977738697, 0.12895076294164143, 0.7802681016871329, 0.8760605852835908, 0.7561866454854006, 0.1, 0.806157473211206, 0.43741508227668313, 0.14499657626758689, 0.5447150204880439, 0.17453791682045305, 0.0, 0.75, 0.9, 0.25, 0.5053362964752235, 0.8426231799651104, 0.3334924579070456, 0.1, 0.11252838250500674, 0.8188695915998453, 0.7647330442246717, 0.9388024540358257, 0.8749884623167106, 0.1, 0.3138121071094775, 0.8071067713919706, 0.10397153616026067, 0.11872273613735573, 0.05219504594310986, 0.1, 0.1846322132330761, 0.46792089791366065, 0.5347323200442153, 0.0809640328393122, 0.9789991886243172, 0.1, 0.07803036367232452, 0.054460587508108116, 0.212687432259843, 0.42126116477959796, 0.3870020971942155, 0.0, 0.75, 0.9, 0.04, 0.2114168087968582, 0.7777630451121885, 0.9611070866100334, 0.1, 0.8357034995602818, 0.18981887577043388, 0.21246444240221296, 0.2042812101193099, 0.7920650671398989, 0.1, 0.8531756685447787, 0.6026983715013965, 0.588580615803111, 0.7144166827375817, 0.629243717779744, 0.1, 0.18137613063225122, 0.39710338921546684, 0.6669174396992308, 0.009940166814421447, 0.3519949479275213, 0.1, 0.3382568022273107, 0.9175688851455703, 0.0477021854841686, 0.9692001897642261, 0.5775310971748207, 0.0, 0.75, 0.9, 0.04, 0.9514877020829735, 0.0666198611648634, 0.19943458631495103, 0.1, 0.23629050522164763, 0.8655433458479832, 0.9451775081354502, 0.35273324310831466, 0.26573240878450377, 0.1, 0.4219588338522091, 0.9164849363334068, 0.08366010008012004, 2.3946619356340904e-05, 0.9679552585428157, 0.1, 0.06633784100042373, 0.5793948146369553, 0.9255625607683609, 0.6070828963421809, 0.7563032439829568, 0.1, 0.11955586121465578, 0.8162097469944222, 0.21165744963422128, 0.2812772690677591, 0.8690007939512121, 0.0, 0.75, 0.49, 0.04, 0.540179824024078, 0.20809538443262643, 0.6194530054227703, 0.1, 0.6892716975602166, 0.22575224417954598, 0.6061350074067936, 0.6475491405003332, 0.9641744450751131, 0.1, 0.601041791094426, 0.8192586348959484, 0.8719763903437489, 0.27282507808324297, 0.2670539127523537, 0.1, 0.6115080869583839, 0.5593023666064466, 0.7895152677183358, 0.671760763475249, 0.5634985287488935, 0.1, 0.3179956109333739, 0.10410890789738714, 0.639611988382182, 0.924059364546232, 0.016167008013725614, 0.0, 0.75, 0.49, 0.04, 0.9715220184455751, 0.541987602234077, 0.9000524452520312, 0.1, 0.16949709131526491, 0.5714143091969465, 0.6712220776431246, 0.1157721857410543, 0.05662141711551105, 0.1, 0.9590398746772547, 0.6257823717821172, 0.3789152130882175, 0.43516657931223024, 0.5526113994572373, 0.1, 0.4844778737890062, 0.48100176719474386, 0.7729961125267742, 0.13299167283095414, 0.12824736338847642, 0.1, 0.35353142389480985, 0.10400866463226799, 0.6397321615470942, 0.9270843011953466, 0.8812695221359063, 0.0, 0.75, 0.49, 0.04, 0.4673866935829071, 0.12641941836705384, 0.09411916536280507, 0.1, 0.6327989618209762, 0.020465934873961977, 0.0002850036214421259, 0.490980218226869, 0.037180739175525224, 0.1, 0.09769686124552446, 0.4831179762335637, 0.7043422028208203, 0.5955160781253755, 0.058161346605305475, 0.1, 0.7391629393467491, 0.5987151394149329, 0.3629114470394159, 0.4954868895091026, 0.5679749375600828, 0.1, 0.8029795352540356, 0.9021659896871091, 0.9028051029331636, 0.7303794968319348, 0.476754140646536, 0.0, 0.9, 0.9, 0.15, 0.6731169797303737, 0.7511161349349889, 0.9015595163228567, 0.1, 0.16583330916959227, 0.7423627924654095, 0.1529165801796556, 0.7148504590514934, 0.08526235040293995, 0.1, 0.1665193437205651, 0.9040051787532333, 0.5702449529300947, 0.2744671743546633, 0.7770323459045637, 0.1, 0.44310928399372096, 0.6556761297428078, 0.2747792224776676, 0.6132382695891907, 0.3985936801411404, 0.1, 0.36720027683534007, 0.7355511256288926, 0.04288021452707513, 0.500424318293824, 0.1778409700283069, 0.0, 0.75, 0.49, 0.04, 0.4559848499970999, 0.9246781930323711, 0.7718916849993114, 0.1, 0.6619230301752597, 0.616103262117245, 0.12045219165077325, 0.004578224597875158, 0.34044265724677136, 0.1, 0.8330790950367241, 0.22907092587961564, 0.15963406395615265, 0.07793000230851577, 0.7498427553309499, 0.1, 0.5401290359495372, 0.14001078590390825, 0.851710052098202, 0.7501664776585751, 0.39901725578178215, 0.1, 0.91517694045385, 0.7721306753642934, 0.28803628868621467, 0.6751158219677433, 0.598181426442332, 0.7, 0.1, 0.7947731796249232, 0.29112299810114184, 0.13535161628349868, 0.5669220649834387, 0.7993013504550341, 0.1, 0.9043871463416578, 0.861509291698794, 0.8694541476824438, 0.9623231283429271, 0.3293476966616701, 0.1, 0.1460491097112162, 0.7772242873565571, 0.44094292029965076, 0.13176782757919558, 0.8174323862633409, 0.1, 0.6754333951396189, 0.22734091836057868, 0.7539309000907807, 0.06402614472781243, 0.5020512923559971, 0.1, 0.09604002287374158, 0.24029712793124836, 0.5755311842167253, 0.6180523362200622, 0.19756663849348266, 0.8, 0.1, 0.6123518765656185, 0.8506403065936052, 0.042452519601084315, 0.41331970729974965, 0.20835289131779033, 0.1, 0.3625554566008107, 0.5017543432845462, 0.5042056616917842, 0.6876836915951094, 0.6650828885321289, 0.1, 0.22498527709330274, 0.9079583158489107, 0.4359424793463369, 0.4494013981218601, 0.008770829187323415, 0.1, 0.3515579702853492, 0.43413443587184875, 0.8812062852998279, 0.9085957515915299, 0.012430984558258307, 0.1, 0.3664600168009984, 0.10500823236152657, 0.0016771392190161327, 0.8697296317114891, 0.7785696017433963, 1.0, 0.75, 0.49, 0.04, 0.49050650916846017, 0.7853656593381481, 0.9756279770474161, 0.1, 0.8296410651412917, 0.40381818516193, 0.749067887271329, 0.8514101368458161, 0.8391739046608461, 0.1, 0.9050399918306015, 0.012242730466631047, 0.4131000623326857, 0.7299338840122748, 0.39819002026695016, 0.1, 0.73506510020335, 0.6790944044108244, 0.21935471129561301, 0.1915817831271971, 0.6422899195097665, 0.1, 0.5855749628736661, 0.30616521077084424, 0.5709124801499694, 0.5481606748975435, 0.44145284818050823], [0.7624432198534157, 0.1757909192900723, 0.861536132644216, 0.7530548597933443, 0.60814645, 0.6, 0.7, 0.04, 0.6140891772488395, 0.5807541623142228, 0.03558600095019493, 0.1, 0.9158882502315971, 0.20939948465328118, 0.07691015914832289, 0.028291311490324267, 0.6887080333928011, 0.1, 0.44524311736461675, 0.45370213704349216, 0.32618300592930305, 0.8705191209422797, 0.6638388034351508, 0.1, 0.7191057237288354, 0.013345470945373306, 0.030431233459085716, 0.6955065082148271, 0.03727392645981453, 0.1, 0.11887950008566062, 0.6253363559880057, 0.6033910276995668, 0.3092120521430084, 0.9705795572774556, 0.0, 0.75, 0.9, 0.25, 0.9082049791697595, 0.3806695576419019, 0.043965605309724376, 0.1, 0.3287472888353209, 0.7571498362012703, 0.24152979622227766, 0.967485037615893, 0.8186752411701351, 0.1, 0.885995743469267, 0.4744891771209859, 0.6760799042030555, 0.670817943110724, 0.02543198800648727, 0.1, 0.738153786830236, 0.49684070652950574, 0.23519230555513804, 0.9495398055409294, 0.08409217813818115, 0.1, 0.1058753314378219, 0.023108822773370674, 0.5023175175885104, 0.5750127114092122, 0.9642428171438174, 0.0, 0.75, 0.9, 0.04, 0.09006353664671862, 0.2073789667522572, 0.40798090179265234, 0.1, 0.23310652408596566, 0.14657700754225467, 0.4716077788327019, 0.9779357456058253, 0.8934765912485214, 0.1, 0.8949990813912245, 0.8609177494890679, 0.964325702315717, 0.6370628095380865, 0.6761330329007101, 0.1, 0.7577754634592991, 0.24646611157975873, 0.7832614101023169, 0.3094551451239347, 0.16024081516420086, 0.1, 0.8317646640830735, 0.4725080701824579, 0.3327495390692716, 0.19695535517686513, 0.38880096025353617, 0.0, 0.75, 0.9, 0.04, 0.6237342107023425, 0.7952137673208289, 0.5770954440073918, 0.1, 0.9397151611363661, 0.3933603739815589, 0.8495036273367227, 0.6140804786149011, 0.6117595332809486, 0.1, 0.22757975497464, 0.14161291789668484, 0.28127368087062476, 0.7265426058185954, 0.7819336852768994, 0.1, 0.3402770786681658, 0.527779217424875, 0.14033845216713214, 0.804439700929175, 0.9704623336828712, 0.1, 0.38129659034016317, 0.3910893601779988, 0.3671842013695952, 0.0017384316558070045, 0.8909863309552338, 0.0, 0.75, 0.49, 0.04, 0.8645646642860294, 0.2401769446721269, 0.20501061494280448, 0.1, 0.9634532988762343, 0.449556636252158, 0.8151199705606447, 0.579438270527315, 0.36714505652162244, 0.1, 0.5115041025909266, 0.7817212549223985, 0.8037492680041266, 0.6703713139717736, 0.25843468957012694, 0.1, 0.15776581908109188, 0.437599288809158, 0.7593409969205366, 0.49706165043759865, 0.8481785070801685, 0.1, 0.5280588463290472, 0.9293236818809986, 0.48252958518667677, 0.5327510868597396, 0.12489265559775486, 0.0, 0.75, 0.49, 0.04, 0.9373349200063382, 0.9458264630767506, 0.45460086010189527, 0.1, 0.4750479328499815, 0.23212563966499344, 0.8395147459433256, 0.6192354966854343, 0.15890015522952416, 0.1, 0.005688142000697893, 0.2179034886208353, 0.4216945787216101, 0.45010577479866754, 0.08309481515224626, 0.1, 0.8247169694676869, 0.20615997209616088, 0.03812160160494049, 0.6576546512604502, 0.991417584247362, 0.1, 0.26969085300307505, 0.8802161276419401, 0.15229279452417377, 0.8061323904874447, 0.4296991381394434, 0.0, 0.75, 0.49, 0.04, 0.04494695152965977, 0.15638861277736682, 0.26790990603489384, 0.1, 0.24437448895294323, 0.4014753519947164, 0.06011653302770381, 0.0405154078985156, 0.4054904148885168, 0.1, 0.38364575912655086, 0.6558884546526341, 0.5051629433345628, 0.6306948956942984, 0.9496654874431197, 0.1, 0.7807548457564263, 0.5715781362514658, 0.7820840868094696, 0.8959980010563403, 0.10168035399536435, 0.1, 0.4800703065022125, 0.1432241724374208, 0.7162556952597964, 0.2272396916035171, 0.40089548533944364, 0.0, 0.9, 0.9, 0.15, 0.6731169797303737, 0.9206811845710042, 0.9032943929706191, 0.1, 0.3559512499712931, 0.9991066393691934, 0.3048030791672248, 0.31558152572474474, 0.10763921508563234, 0.1, 0.5140463680325451, 0.5176661778157237, 0.48474724567387983, 0.3846584796932244, 0.051934660345886896, 0.1, 0.9248495889888166, 0.9372910416308592, 0.7525013503427251, 0.8057184456776326, 0.9051738458071878, 0.1, 0.4890788595189084, 0.2795057574391938, 0.41679617745014097, 0.2805716296229037, 0.24539437026266864, 0.0, 0.75, 0.49, 0.04, 0.8901130097266082, 0.4984944460813786, 0.9043758148640746, 0.1, 0.3200363708065683, 0.9381210030648869, 0.5915784702432205, 0.872368163449692, 0.494240372179259, 0.1, 0.0449320269297514, 0.03968558281049406, 0.40048448943910875, 0.5690558729551383, 0.20617517211156366, 0.1, 0.636755085623129, 0.5653211068770606, 0.8879877603757286, 0.9488068423333437, 0.06906792210306967, 0.1, 0.8743568849291063, 0.7049447400984516, 0.18745245069964334, 0.3487631730102777, 0.6440647182902303, 0.7, 0.1, 0.4114400346453574, 0.9104281498615308, 0.8776192808048198, 0.4584899398483717, 0.457924279427671, 0.1, 0.8277957622041574, 0.07368233041977956, 0.1906935818236789, 0.7489855988771219, 0.6863901667838915, 0.1, 0.8440200455228851, 0.7552216508001908, 0.6221744176696146, 0.5405236902796419, 0.9561856605079334, 0.1, 0.6644625793403848, 0.6701450403361231, 0.36443616754645203, 0.773893532210233, 0.31314345250594444, 0.1, 0.43828454566868114, 0.14696110879688895, 0.19772604338455002, 0.899166034257871, 0.46504321771107293, 0.8, 0.1, 0.7767098961147645, 0.574935041813986, 0.1788661052127335, 0.211696023445882, 0.7186499648108438, 0.1, 0.6777946950919302, 0.19314166335598637, 0.8302989962205852, 0.29602166230946747, 0.3892692503597389, 0.1, 0.9074865850942057, 0.939605797032917, 0.9835460705512786, 0.3149163541105343, 0.0017082666391389223, 0.1, 0.45998730858295434, 0.14944116103631166, 0.08816394136454497, 0.18248906853150593, 0.42018407308827277, 0.1, 0.9570448972875827, 0.4887552426669326, 0.7923170912471283, 0.28415633330718026, 0.09977068906942199, 1.0, 0.75, 0.49, 0.04, 0.95363904272984, 0.27803145740956503, 0.7211978362799071, 0.1, 0.09076234861946098, 0.6296230869046885, 0.45397480150607294, 0.04427010329967207, 0.154395976910501, 0.1, 0.9685437833315841, 0.19624221357406935, 0.27097690564470633, 0.5993692179957295, 0.7945413654695462, 0.1, 0.06725165947050737, 0.070482319077106, 0.9921919201159592, 0.22348515649708445, 0.11675046689019275, 0.1, 0.048100783037544836, 0.5904553421051553, 0.12333094231654163, 0.046550607144371425, 0.88358133149365], [0.5710408771005147, 0.4578478109401851, 0.5145287124789838, 0.31567825025130003, 0.60814645, 0.6, 0.7, 0.04, 0.3288769481036877, 0.872788339446428, 0.6231621674617225, 0.1, 0.6586237313074812, 0.05125319363351921, 0.7271285553465746, 0.12067752185369396, 0.48597838357330403, 0.1, 0.1918268475552286, 0.09636216873824044, 0.6700487738812799, 0.42975269389491866, 0.1732562261156726, 0.1, 0.0527854290062747, 0.42247879841849, 0.23014762603396588, 0.25161736134661916, 0.15206729502358862, 0.1, 0.9663315675983724, 0.9244109584111914, 0.6448186069320567, 0.26658938889500594, 0.18883926808401352, 0.0, 0.75, 0.9, 0.25, 0.03468492945763413, 0.2191414696092041, 0.660969598618791, 0.1, 0.2416507888795273, 0.2645782471865483, 0.14437192483952677, 0.426000167014025, 0.03375518813049949, 0.1, 0.9776294548678168, 0.9521767604783314, 0.6924497120727958, 0.7454049459711468, 0.5107110799484317, 0.1, 0.2735992265985615, 0.9733215909165499, 0.7329573691003829, 0.577195104216453, 0.326695808295427, 0.1, 0.896210540552659, 0.2641631622090691, 0.38716204965364964, 0.5181684928041355, 0.12041666484323321, 0.0, 0.75, 0.9, 0.04, 0.8560232288965621, 0.5013676157273247, 0.3046713764903035, 0.1, 0.552241353158136, 0.6282286022309522, 0.5634723646211717, 0.7428971810780073, 0.5560564505137614, 0.1, 0.9300253883365124, 0.5020379681979549, 0.13342126208096738, 0.22721950689928472, 0.7669815903311962, 0.1, 0.5380520839756499, 0.002126954198614639, 0.06960831721943883, 0.4319899430174874, 0.43629145551257364, 0.1, 0.9625181611025012, 0.9717768531874068, 0.2437418671071011, 0.8143154927236267, 0.8677027984870174, 0.0, 0.75, 0.9, 0.04, 0.4754928310484793, 0.7971581464088577, 0.9655121033746288, 0.1, 0.8736276633982106, 0.43411413724537795, 0.2182377242033834, 0.05894963574246448, 0.2577130979900615, 0.1, 0.18225923542641786, 0.9437896138150155, 0.28024961908291746, 0.9676587958419383, 0.032516286996315924, 0.1, 0.14398468396668151, 0.5387878952461871, 0.41884324332957223, 0.07920099909849576, 0.8757578625139831, 0.1, 0.29951213316959824, 0.8141674165268007, 0.010472037733430972, 0.6538714914990851, 0.6254520659258819, 0.0, 0.75, 0.49, 0.04, 0.5906307491454087, 0.2658218239789665, 0.9241330947973613, 0.1, 0.12498133629171826, 0.995785771711305, 0.0014155501746002663, 0.9537422443600329, 0.3368191914791122, 0.1, 0.899070309902016, 0.5034372136668553, 0.26549255705539965, 0.23180294075539176, 0.542634336133269, 0.1, 0.41331714970064937, 0.03073654361998046, 0.427529858837039, 0.6537007860200236, 0.762198620274143, 0.1, 0.2960674353080731, 0.04428864429327728, 0.22979451086635316, 0.05350645366370177, 0.08821483983887757, 0.0, 0.75, 0.49, 0.04, 0.5361675475414073, 0.1967261187292917, 0.6889779413431711, 0.1, 0.15076336859118955, 0.7103282900847833, 0.7343747773360124, 0.45303793267941417, 0.5106090770976613, 0.1, 0.8316063435327797, 0.09289494111609276, 0.0814590434276915, 0.6848800942430556, 0.1495313513012737, 0.1, 0.21291900367227445, 0.8166557124116239, 0.2714467934292454, 0.6456453079950973, 0.5710716219584964, 0.1, 0.2410808740292334, 0.6928740716241379, 0.17849495776299829, 0.25622767612870423, 0.8602602159999342, 0.0, 0.75, 0.49, 0.04, 0.24205087668256442, 0.29748788816737726, 0.8846071818900708, 0.1, 0.8017036803128059, 0.48261652620320916, 0.9344603755155746, 0.01897571286648725, 0.08388599892272663, 0.1, 0.5799952550539897, 0.31892102736406247, 0.7086419006240487, 0.3226323439636063, 0.20063912911609116, 0.1, 0.31398017126074307, 0.23933011053271758, 0.249043112193156, 0.19131122772631137, 0.7928526927494476, 0.1, 0.2647441914763832, 0.7690460953839602, 0.7670598565756797, 0.9654405428408175, 0.47013388863856087, 0.0, 0.9, 0.9, 0.15, 0.6731169797303737, 0.6373924031616164, 0.6046011340767758, 0.1, 0.4533348196775582, 0.8191585843840622, 0.048847915892390015, 0.46445863222270833, 0.18002244459990513, 0.1, 0.984728315435413, 0.03678139682201231, 0.9907460100456692, 0.3125741889544984, 0.9265972225525235, 0.1, 0.19209362473701874, 0.7582967606117922, 0.8321730035131848, 0.4588069570527247, 0.05286364896012452, 0.1, 0.9348126073499757, 0.058910098946601375, 0.1865894615068585, 0.5818560077168474, 0.028953427694995093, 0.0, 0.75, 0.49, 0.04, 0.44324806497341385, 0.2134575668351648, 0.6132014034566438, 0.1, 0.3606362685076828, 0.8098297194162504, 0.9015966803580165, 0.3050083367999401, 0.1285594024508353, 0.1, 0.7333489123214781, 0.5447952945807285, 0.06844827460826619, 0.750746652515309, 0.20754924874356406, 0.1, 0.3594767887748238, 0.6513603543613061, 0.14875981724250587, 0.13505268314182806, 0.43968394142999845, 0.1, 0.9290235389480164, 0.47502766914719563, 0.07523641135930415, 0.8244292222521369, 0.4968169297154321, 0.7, 0.1, 0.04108615213914446, 0.4356624006376012, 0.9705045600013498, 0.5290255920523053, 0.7692965378909115, 0.1, 0.461586722127429, 0.6880766233444624, 0.8797274324516177, 0.004947813712931293, 0.3313264582445311, 0.1, 0.48586502042973534, 0.5336090753658271, 0.7309256995464727, 0.8911530291988197, 0.5423696011186271, 0.1, 0.188668036453986, 0.6786074214817011, 0.8400029039622521, 0.021470505874184087, 0.5227346141819975, 0.1, 0.07709199239375708, 0.2728740034512319, 0.37634185374030116, 0.9197425997196103, 0.766304436441288, 0.8, 0.1, 0.3948643082886505, 0.09024531613577991, 0.301870626379129, 0.8490856073726758, 0.3637105100374539, 0.1, 0.6620195220401349, 0.8173310162858981, 0.4284965441158233, 0.3532646036230104, 0.5974722175134607, 0.1, 0.7271843509724064, 0.04366631459414405, 0.25353342215320274, 0.43572589540988493, 0.7302734293211343, 0.1, 0.038583701048968466, 0.7193761179156148, 0.5982006051225611, 0.1860316718923678, 0.04318285670654154, 0.1, 0.32543365258705215, 0.2179021191771633, 0.013997832772996932, 0.45477202012218876, 0.9033717313068516, 1.0, 0.75, 0.49, 0.04, 0.7920703381316371, 0.01356298375404752, 0.7626748814322957, 0.1, 0.21913053971773389, 0.3571308999296826, 0.13025732768110176, 0.2013019008625866, 0.3082508533981584, 0.1, 0.7996718770975215, 0.8361364212154566, 0.924497927932835, 0.5134116376855284, 0.8486173234057559, 0.1, 0.5583200387828997, 0.18067793325429393, 0.13391396179898207, 0.13229744806361365, 0.4818580223956177, 0.1, 0.7160880677098688, 0.5433840551255534, 0.7041467511604266, 0.15501525033254815, 0.9934410767148815]]
  for rsi in range(meta['max_rs_iter']): # for rsi in range(len(best_chroms_sofar)):

    if args['verbose_meta']:
      print('Search iteration {}.'.format(rsi+1))

    num_chrom_param = comp_num_chrom_param(limits)

    # a_rand_chrom = gen_rand_chromosome(num_chrom_param)

    # a_rand_chrom = np.array(best_chroms_sofar[rsi])

    # a_rand_chrom = np.asarray([rsi/meta['max_rs_iter'] for i in range(num_chrom_param)])
    # for i in range(1, num_chrom_param, 2):
    #   a_rand_chrom[i] = 1 - a_rand_chrom[i]

    # if rsi%3 == 0:
    #   a_rand_chrom = np.array([0.475 for i in range(num_chrom_param)])
    # elif rsi%3 == 1:
    #   a_rand_chrom = np.array([0.49 for i in range(num_chrom_param)])
    #   rand_vec = [random.random() * 0.02 - 0.01 for i in range(num_chrom_param)]
    #   a_rand_chrom = np.add(a_rand_chrom, rand_vec)
    # else:
    #   a_rand_chrom = np.array([0.425 for i in range(num_chrom_param)])

    # a_rand_chrom = np.array([0.49 for i in range(num_chrom_param)])
    # rand_vec = [random.random() * 0.02 - 0.01 for i in range(num_chrom_param)]
    # a_rand_chrom = np.add(a_rand_chrom, rand_vec)

    a_rand_chrom = np.asarray([rsi/meta['max_rs_iter'] for i in range(num_chrom_param)])

    # a_rand_chrom = np.array([(1-rsi/meta['max_rs_iter'])*0.9 for i in range(num_chrom_param)])

    # a_rand_chrom = np.asarray([rsi/meta['max_rs_iter'] + (random.random()*(1/meta['max_rs_iter'])) for i in range(num_chrom_param)])

    # a_rand_chrom = np.array([(3/22)+(random.random()*(1/22)) for i in range(num_chrom_param)])

    # --- Actual training
    model, train_params = prepare_model(a_rand_chrom)
    model, valid_accurs, train_accurs = do_eval_chrom(model, train_params, args['num_epochs_search'])
    best_valid_accur = max(valid_accurs)
    print('Best validation accuracy: {}%.'.format(best_valid_accur))

    # Store best model
    if best_valid_accur > best_model_accur:
      best_model_accur = best_valid_accur
      best_model = model
      best_train_params = train_params
      best_chromosome = a_rand_chrom

    meta_rs_valids.append(best_valid_accur)
    # adds the generated chromosome with its accuracy into the list
    mat_chroms[rsi][0] = best_valid_accur
    mat_chroms[rsi][1:] = a_rand_chrom
  
  # # sorted list of chromosomes in the initial search by performance (accuracy)
  # mat_chroms = sorted(mat_chroms, reverse=True)
  print('*****************************************************{}'.format(ti))
  print('Best accuracy after initial random search: {}'.format(best_model_accur))
  print('*****************************************************')

  if args['verbose_meta']:
    print('Best validation errors:')
    print(meta_rs_valids)

  num_chrom_params = best_chromosome.shape[0]
  next_best_initial_chrom_ind = 1

  best_res = (best_model_accur, best_chromosome, best_model, best_train_params)

  # -----------------------------------------------------------------------------------------------------------
  # population-based search

  # generate initial direction vector for each chromosome
  direction_vec = [np.random.rand(num_chrom_param)*0.2 for i in range(meta['max_rs_iter'])]
  direction_vec = [[i[j]-0.1 for j in range(len(direction_vec[0]))] for i in direction_vec] # [i.tolist() for i in direction_vec]

  good_solutions = []
  bad_solutions = []
  good_sol_thresh = 5
  bad_sol_thresh = 10 # 5
  good_sol_radius = 6 # 11.5  # 6  # impact radius for good solutions
  bad_sol_radius = 6  # 11.5  # 6  # impact radius for bad solutions

  accuracy_thresh = 12.0

  for i, a_chrom in enumerate(mat_chroms):
    if a_chrom[0] <= accuracy_thresh:
      bad_solutions.append(a_chrom)
    else:
      good_solutions.append(a_chrom)

  # sorted_mat_chroms = sorted(mat_chroms, reverse=True)
  # good_solutions.append(sorted_mat_chroms[0])
  # bad_solutions.append(sorted_mat_chroms[len(sorted_mat_chroms)-1])

  # bad_solutions = sorted_mat_chroms[0:math.floor(len(sorted_mat_chroms)/2)]
  # good_solutions = sorted_mat_chroms[math.floor(len(sorted_mat_chroms)/2):]

  evaluated_chroms = []

  terminate_search = False
  pop_search_iter = 1

  has_good_sol = False
  bad_solution_counter = 0 #(2)
  while not terminate_search:
    thresh_increase_rate = 1.0
    print("========================================================={}".format(ti))
    print("population search iteration: {}".format(pop_search_iter))
    print("=========================================================")

    # --- loosely based ant colony optimisation with pheromone and anti-pheromone mechanisms

    # initialize chromosomes
    # evaluate chromosomes
    # add chromosomes to bad and good solutions pools based on their performance
    # initialize trajectory (may be changed to zero, depends)
    # set counter for bad solutions for each chromosome to 0 (2)
    # for each iteration, do
    #   for each chomosome in population do,
    #     for each chromosome in both bad and good solutions pools, do
    #       calculate distance between chromosome and the solutions
    #       manipulate trajectory based on distance
    #     end for
    #     add new trajectory to chromosome
    #     evaluate new chromosome
    #     if chromosome is bad solution, then
    #       add to bad solution
    #       if chromosome is bad solution for n iterations, then (2)
    #         use differential search to generate new chromosome (2)
    #         replace current chromosome with new chromosome (2)
    #         set trajectory to 0 (2)
    #         reset iteration counter (2)
    #       otherwise (2)
    #         increment bad solutions counter for the chromosome (2)
    #       end if (2)
    #     otherwise
    #       do local search
    #       add best result from local search to good solution
    #       use differential search to generate new chromosome (1) #----- debatable
    #       replace current chromosome with new chromosome (1)
    #       set trajectory to 0 (1)
    #     end if
    #   end for
    #   if iteration has no good solutions, then (3)
    #     do differential search to generate new population, based on performance of chromosome (3)
    #     set trajectory to 0 (3)
    #   end if (3)
    # end for

    do_diff_search = False
    new_chromosomes = []
    for i, a_chrom in enumerate(mat_chroms):
      closest_dis = 19.4  # approximate Euclidean distance between two farthest points in the search space
      closest_good = False
      closest_bad = False

      # for j, sol in enumerate(good_solutions):
      #   if np.all(np.equal(a_chrom, sol)):
      #     continue
      #   # calculate Euclidean distance between the two chomosomes
      #   # source: https://stackoverflow.com/a/50639386
      #   euclid_dis = sum((p-q)**2 for p, q in zip(a_chrom[1:], sol[1:])) ** 0.5
      #   # if the chromosome is close enough to a good solution, regardless of distance
      #   # set the good solution as the closest 
      #   if euclid_dis <= good_sol_radius and euclid_dis < closest_dis:
      #     closest_dis = euclid_dis
      #     closest_chrom = sol[1:]
      #     closest_good = True
      
      # # calculate Euclidean distance between current chromosome and good_sol / bad_sol
      # for j, sol in enumerate(bad_solutions):
      #   if closest_good:
      #     break
      #   if np.all(np.equal(a_chrom, sol)):
      #     continue
      #   # calculate Euclidean distance between the two chomosomes
      #   euclid_dis = sum((p-q)**2 for p, q in zip(a_chrom[1:], sol[1:])) ** .5
      #   # if the chromosome is within radius of a bad solution and the solution is the closest to it,
      #   # replace with new closest solution, otherwise ignore
      #   if euclid_dis <= bad_sol_radius and euclid_dis < closest_dis:
      #     closest_dis = euclid_dis
      #     closest_chrom = sol[1:]
      #     closest_bad = True
      
      # # used in order to get the gradient intersecting x=0 at 3 and x=radius at a value < 0.5, where radius is the good/bad solution radius
      # multiply_factor = 3 - closest_dis/2.2

      # if closest_good:
      #   # apply force to change direction of chromosome towards solution
      #   force_vec = np.subtract(a_chrom[1:], closest_chrom)
      #   force_vec = np.multiply(multiply_factor, force_vec)
      #   # inverse scaling to generate larger force the closer a chromosome is to a solution
      #   direction_vec[i] = np.add(direction_vec[i], force_vec)
      #   # np.clip(direction_vec[i], 0, 0.4, out=direction_vec[i])
      #   new_chrom = np.add(a_chrom[1:], direction_vec[i])
      # elif closest_bad:
      #   # apply force to change direction of chromosome away from solution
      #   force_vec = np.subtract(a_chrom[1:], closest_chrom)
      #   force_vec *= -1
      #   force_vec = np.multiply(multiply_factor, force_vec)
      #   # inverse scaling to generate larger force the closer a chromosome is to a solution
      #   direction_vec[i] = np.add(direction_vec[i], force_vec)
      #   # np.clip(direction_vec[i], 0, 0.4, out=direction_vec[i])
      #   new_chrom = np.add(a_chrom[1:], direction_vec[i])
      # else:
      #   # retain trajectory
      #   new_chrom = np.add(a_chrom[1:], direction_vec[i])

      # ------------------------------------------------------------------
      # CHANGES MADE
      # ------------------------------------------------------------------
      # all good and bad solutions will influence the current chromosome, not just the closest
      for j, sol in enumerate(good_solutions):
        if np.all(np.equal(a_chrom, sol)):
          continue
        # calculate Euclidean distance between the two chomosomes
        # source: https://stackoverflow.com/a/50639386
        euclid_dis = sum((p-q)**2 for p, q in zip(a_chrom[1:], sol[1:])) ** 0.5
        diff_vec = np.subtract(sol[1:], a_chrom[1:])
        force_vec = np.divide(diff_vec, 5/euclid_dis) #?
        force_ratio = force_vec * (sol[0]/100)
        direction_vec[i] = np.add(direction_vec[i], force_ratio)
        # force_rate = (sol[0] / accuracy_thresh) - 1
        # direction_vec[i] = direction_vec[i] * force_rate
      
      for j, sol in enumerate(bad_solutions):
        if np.all(np.equal(a_chrom, sol)):
          continue
        # calculate Euclidean distance between the two chomosomes
        # source: https://stackoverflow.com/a/50639386
        euclid_dis = sum((p-q)**2 for p, q in zip(a_chrom[1:], sol[1:])) ** 0.5
        diff_vec = np.subtract(sol[1:], a_chrom[1:])
        diff_vec = diff_vec * -1
        force_vec = np.divide(diff_vec, 5/euclid_dis) #?
        force_ratio = force_vec * (sol[0]/100)
        direction_vec[i] = np.add(direction_vec[i], force_ratio)
        # force_rate = 1 - (sol[0] / accuracy_thresh)
        # direction_vec[i] = direction_vec[i] * force_rate

      new_chrom = np.add(a_chrom[1:], direction_vec[i])
      


      # # wrap around
      # for k in range(len(new_chrom)):
      #   if new_chrom[k] == 1.0:
      #     new_chrom[k] = 0.00000000001
      #   elif new_chrom[k] == 0.0:
      #     new_chrom[k] = 0.99999999999
      #   elif new_chrom[k] > 1.0 or new_chrom[k] < 0.0:
      #     new_chrom[k] -= math.floor(new_chrom[k])
      # # bounce back
      # for k in range(len(new_chrom)):
      #   if new_chrom[k] == 1.0:
      #     new_chrom[k] = 0.99999999999
      #   elif new_chrom[k] == 0.0:
      #     new_chrom[k] = 0.00000000001
      #   elif new_chrom[k] > 1.0 or new_chrom[k] < 0.0:
      #     new_chrom[k] -= math.ceil(new_chrom[k])

      # ------------------------------------------------------------------
      # CHANGES MADE
      # ------------------------------------------------------------------
      # damping effect - this is to: 
      # (1) prevent overflow/underflow of param values,
      # (2) allows chromosome to stay in roughly the same area and remain on trajectory (prevent extreme perturbation like in wraparound)
      # (3) similar to bounce back, allows chromosome to remain around the intended area, but is more faithful to the trajectory
      # (4) prevent getting stuck on search space as np.clip tends to do
      for k in range(len(new_chrom)):
        if new_chrom[k] == 1.0:
          new_chrom[k] = 0.99999999999
        elif new_chrom[k] == 0.0:
          new_chrom[k] = 0.00000000001
        elif new_chrom[k] > 1.0:
          new_chrom[k] = new_chrom[k] - direction_vec[i][k]
          adjustment = (1.0 - new_chrom[k]) * 0.5
          new_chrom[k] += adjustment
        elif new_chrom[k] < 0.0:
          new_chrom[k] = new_chrom[k] - direction_vec[i][k]
          adjustment = new_chrom[k] * 0.5
          new_chrom[k] -= adjustment

      # np.clip(new_chrom, 0, 0.99999999999, out=new_chrom)
      new_chromosomes.append(new_chrom)
    
    # test validation accuracy of new chromosomes
    new_res, evaluated_chroms = eval_chromosomes(new_chromosomes, num_chrom_params)
    if new_res[0] > best_res[0]:# and random.random() < 0.8:  # 30% to not replace current best with new best to avoid overfitting
      thresh_increase_rate += (new_res[0] - best_res[0])/5
      best_res = new_res
    
    for i, a_chrom in enumerate(evaluated_chroms):
      # if solution generated is worse or equal to the threshold
      if a_chrom[0] <= accuracy_thresh:
        if len(bad_solutions) >= bad_sol_thresh:
          bad_solutions[bad_solutions.index(max(bad_solutions))] = a_chrom.tolist()
        else:
          bad_solutions.append(a_chrom.tolist())
        
        # if bad_solution_counter[i] >= 4: #(2)
        #   # differential search #(2)
        #   direction_vec[i] = [0.0 for i in range(num_chrom_params)] #(2)
        #   bad_solution_counter[i] = 0 #(2)
        # else: #(2)
        #   bad_solution_counter[i] += 1 #(2)
        bad_solution_counter += 1
      else:
        has_good_sol = True
        # perform local search when in a potentially good location
        new_chrom = local_search(a_chrom, mat_chroms[i], num_chrom_params, pop_search_iter)
        # differential search #(1)
        # print(new_chrom)
        # evaluated_chroms[i] = differential_search_v1(new_chrom, good_solutions, bad_solutions, num_chrom_params)
        # direction_vec[i] = np.array([0.0 for i in range(num_chrom_params)]) #(1)
        evaluated_chroms[i] = new_chrom # replace with chromosome from differential search #(1)

        if len(good_solutions) >= good_sol_thresh:
          good_solutions[good_solutions.index(min(good_solutions))] = new_chrom.tolist()
        else:
          good_solutions.append(new_chrom.tolist())
      
      accuracy_thresh += thresh_increase_rate
      thresh_increase_rate = 0.0
      print("=========================================================")
      print("*********************************************************")
      print("accuracy threshold: {}".format(accuracy_thresh))
      print("*********************************************************")
      print("*********************************************************")
      print("=========================================================")
      # if accuracy_thresh > 50.0:
      #   accuracy_thresh = 50.0
    
    if not has_good_sol:
      # increase good solution radius, decrease bad solution radius
      good_sol_radius /= 0.9
      bad_sol_radius *= 0.9

      # # differential search #(3)
      # new_population = differential_search(evaluated_chroms, good_solutions, bad_solutions, num_chrom_params) #(3)
      # do_diff_search = True #(3)

      # counter for resetting
      # reset_countdown -= 1
    else:
      # reset radius
      good_sol_radius = 6
      bad_sol_radius = 6
    
    if bad_solution_counter >= 9:
      # differential search
      evaluated_chroms = differential_search_v2(evaluated_chroms, good_solutions, bad_solutions, num_chrom_params)
      bad_solution_counter = 0
    
    mat_chroms = evaluated_chroms

    has_good_sol = False
    thresh_increase_rate += 1
    # accuracy_thresh += thresh_increase_rate
    
    # if do_diff_search:
    #   mat_chroms = new_population
    # else:
    #   mat_chroms = evaluated_chroms
    



    # initialize population
    # evaluate population
    # store global best
    # initialize direction_vector of each chromosome
    # while not terminating, do
    #   
    #   for each chromosome in population, do
    #     if chromosome within radius of a good solution, then
    #       apply force to change direction of chromosome towards solution
    #     else if chromosome within radius of a bad solution, then
    #       apply force to change direction of chromosome away from solution
    #       (perform differential search between current chromosome and ...)                *****
    #       (remove chromosome and add chromosome based on differential search between ...) *****
    #     else
    #       retain trajectory
    #     end if 
    #   end for
    #   
    #   evaluate newly generated chromosomes
    #   for each chromosome, do
    #     if solution is worse (or the same), then
    #       if list of bad solutions > threshold, then
    #         replace oldest (best) bad solutions with latest bad solutions
    #       else
    #         add to list of bad solutions
    #       end if
    #     else
    #       if list of good solutions > threshold, then
    #         replace oldest (worst) good solutions with latest good solutions
    #       else
    #         add to list of good solutions
    #       end if
    #       perform local search on chromosome (use random walking technique)
    #     end if
    #   end for
    #   
    #   (increase redius of good and bad solutions)
    # end while


    print('======================================{}'.format(ti))
    print('Best accuracy so far: {}'.format(best_res[0]))
    print('======================================')

    pop_search_iter += 1
      

  # -----------------------------------------------------------------------------------------------------------
  # final evaluation

  # ==========================
  # Test final model / Visualize predictions
  # ==========================

  # Final visualization
  # print('======================================')
  # print('Best accuracy so far: {}'.format(best_res[0]))
  # print('======================================')
  print('Computing the final test ...')
  
  best_model_accur, best_chromosome, best_model, best_training_params = best_res
  best_final_model, best_validation_model, best_valid_accurs, best_train_accurs = do_training(best_model, best_training_params, args['num_epochs_test'])

  print('=====================================')
  print('Training accuracies')
  print('=====================================')
  print(best_train_accurs)
  print('=====================================')
  print('Best training parameters')
  print('=====================================')
  print(best_training_params)
  print('=====================================')
  print('Best model')
  print('=====================================')
  print(best_validation_model)
  print('=====================================')
  print('Test accuracies')
  print('=====================================')
  print('Model with best validation accuracy: ')
  accur_valid, test_loss = final_test(best_validation_model)
  all_average_loss[0].append(test_loss.item())
  print('Model at the end of training: ')
  accur_final, test_loss = final_test(best_final_model)
  all_average_loss[1].append(test_loss.item())

  # max_accur = int(max(accur_valid, accur_final))
  # print("The max accuracy is: {}".format(max_accur))

  best_models_list.append(best_validation_model)
  best_chromosomes_list.append(best_chromosome)
  best_train_params_list.append(best_training_params)

  all_accur_valid.append(accur_valid)
  all_accur_final.append(accur_final)
    
valid_accuracies = [i.item() for i in all_accur_valid]
final_accuracies = [i.item() for i in all_accur_final]
print("All valid accuracies: {}".format(valid_accuracies))
print("All final accuracies: {}".format(final_accuracies))
print("All average validation loss: {}".format(all_average_loss[0]))

print("Mean accuracy:         {}".format(np.mean(valid_accuracies)))
print("Standard deviation:    {}".format(np.std(valid_accuracies)))
print("Minimum accuracy:      {}".format(min(valid_accuracies)))
print("1st Quartile accuracy: {}".format(np.percentile(valid_accuracies, 25)))
print("Median accuracy:       {}".format(np.median(valid_accuracies)))
print("3rd Quartile accuracy: {}".format(np.percentile(valid_accuracies, 75)))
print("Maximum accuracy:      {}".format(max(valid_accuracies)))
print("Interquartile range:   {}".format(np.percentile(valid_accuracies, 75) - np.percentile(valid_accuracies, 25)))
print("Total time taken: {}".format((time.time()-starting_time)))

print("Do eval calls per trial: {}".format(do_eval_iter))
print("Take previous: {}".format(testing['take_previous']))
print("Take random: {}".format(testing['take_random']))
print("All best models: {}".format(best_models_list))
print("All best chromosomes: {}".format(best_chromosomes_list))
print("All best training parameters: {}".format(best_train_params_list))
# print("Population search iterations per trial: {}".format(pop_search_iter))
# print("Local search iterations per trial: {}".format(local_search_iter))
# print("Differential search iterations per trial: {}".format(diff_search_iter))

# --- Saving the best chromosome as a csv file
if args['save_best_chrom']:

  print('Saving the best chromosome in the root directory ...')

  from datetime import datetime
  from numpy import asarray
  from numpy import savetxt
  import numpy as np

  def get_date_time_str():
    now = datetime.now() # current date and time
    return now.strftime("%d%m%Y_%H%M")

  # Create filename
  path = './'
  filename = get_date_time_str() + "_" + str(max_accur)
  savetxt(path+filename, best_chromosome, delimiter=',')
